---

# Runtime secrets. Values are base64 encoded

kind: Secret
apiVersion: v1
metadata:
  name: ztc-secrets
  namespace: zgw
type: Opaque
data:
  SECRET_KEY: {{ secret_keys.ztc | b64encode }}
  DB_PASSWORD: {{ db_password | b64encode }}
  SENTRY_DSN: {{ sentry_dsns.ztc | b64encode }}
