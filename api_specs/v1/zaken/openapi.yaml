openapi: 3.0.4
info:
  title: Zaken API
  version: 1.5.1
  description: |
    <b><i>Met expand mechanisme in plain json</i></b>

    Een API om een zaakregistratiecomponent (ZRC) te benaderen.

    De ZAAK is het kernobject in deze API, waaraan verschillende andere
    resources gerelateerd zijn. De Zaken API werkt samen met andere API's voor
    Zaakgericht werken om tot volledige functionaliteit te komen.

    **Wat is nieuw?**
    * Query parameters en zoekvelden voor archivering.

    **Afhankelijkheden**

    Deze API is afhankelijk van:

    * Catalogi API
    * Notificaties API
    * Documenten API *(optioneel)*
    * Besluiten API *(optioneel)*
    * Autorisaties API *(optioneel)*

    **Autorisatie**

    Deze API vereist autorisatie. Je kan de
    [token-tool](https://zaken-auth.vng.cloud/) gebruiken om JWT-tokens te
    genereren.

    ### Notificaties

    Deze API publiceert notificaties op het kanaal `zaken`.

    **Main resource**

    `zaak`

    **Kenmerken**

    * `bronorganisatie`: Het RSIN van de Niet-natuurlijk persoon zijnde de organisatie die de zaak heeft gecreeerd. Dit moet een geldig RSIN zijn van 9 nummers en voldoen aan https://nl.wikipedia.org/wiki/Burgerservicenummer#11-proef
    * `zaaktype`: URL-referentie naar het ZAAKTYPE (in de Catalogi API) in de CATALOGUS waar deze voorkomt
    * `vertrouwelijkheidaanduiding`: Aanduiding van de mate waarin het zaakdossier van de ZAAK voor de openbaarheid bestemd is.

    **Resources en acties**
    - `zaak`: create, update, destroy
    - `status`: create
    - `zaakobject`: create, update, destroy
    - `zaakinformatieobject`: create
    - `zaakeigenschap`: create, update, destroy
    - `klantcontact`: create
    - `rol`: create, destroy
    - `resultaat`: create, update, destroy
    - `zaakbesluit`: create
    - `zaakcontactmoment`: create
    - `zaakverzoek`: create

    **Handige links**

    * [Documentatie](https://vng-realisatie.github.io/gemma-zaken/standaard)
    * [Zaakgericht werken](https://vng-realisatie.github.io/gemma-zaken)
  contact:
    url: https://github.com/VNG-Realisatie/gemma-zaken
    email: standaarden.ondersteuning@vng.nl
  license:
    name: European Union Public Licence (EUPL-1.2)
    url: https://interoperable-europe.ec.europa.eu/collection/eupl/eupl-text-eupl-12
servers:
  - url: https://{host}/{basePath}/v1
    variables:
      host:
        default: example.com
        description: De hostserver voor de API
      basePath:
        default: api
        description: Het basispad voor de API
paths:
  /klantcontacten:
    get:
      operationId: klantcontact_list
      description: |
        Alle KLANTCONTACTen opvragen.

        **DEPRECATED**: gebruik de contactmomenten API in plaats van deze endpoint.
      summary: "Alle KLANTCONTACTen opvragen. "
      parameters:
        - name: zaak
          required: false
          in: query
          description: URL-referentie naar de ZAAK.
          schema:
            type: string
            format: uri
        - name: page
          required: false
          in: query
          description: Een pagina binnen de gepagineerde set resultaten.
          schema:
            type: integer
      tags:
        - klantcontacten
      security:
        - JWT-Claims:
            - zaken.lezen
      deprecated: true
      responses:
        "200":
          headers:
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedKlantContactList"
          description: OK
        "400":
          $ref: '#/components/responses/ValidatieFoutResponse'
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
    post:
      operationId: klantcontact_create
      description: |
        Indien geen identificatie gegeven is, dan wordt deze automatisch gegenereerd.

        **DEPRECATED**: gebruik de contactmomenten API in plaats van deze endpoint.
      summary: Maak een KLANTCONTACT bij een ZAAK aan.
      parameters:
        - in: header
          name: Content-Type
          schema:
            type: string
            enum:
              - application/json
          description: Content type van de verzoekinhoud.
          required: true
        - in: header
          name: X-NLX-Logrecord-ID
          schema:
            type: string
          description: Identifier of the request, traceable throughout the network
        - in: header
          name: X-Audit-Toelichting
          schema:
            type: string
          description: Toelichting waarom een bepaald verzoek wordt gedaan
      tags:
        - klantcontacten
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/KlantContact"
        required: true
      security:
        - JWT-Claims:
            - (zaken.bijwerken | zaken.geforceerd-bijwerken)
      deprecated: true
      responses:
        "201":
          headers:
            Location:
              $ref: '#/components/headers/LocationHeader'
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KlantContact"
          description: Created
        "400":
          $ref: '#/components/responses/ValidatieFoutResponse'
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
  /klantcontacten/{uuid}:
    get:
      operationId: klantcontact_retrieve
      description: |
        Een specifieke KLANTCONTACT opvragen.

        **DEPRECATED**: gebruik de contactmomenten API in plaats van deze endpoint.
      summary: Een specifieke KLANTCONTACT opvragen.
      parameters:
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
      tags:
        - klantcontacten
      security:
        - JWT-Claims:
            - zaken.lezen
      deprecated: true
      responses:
        "200":
          headers:
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KlantContact"
          description: OK
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
  /resultaten:
    get:
      operationId: resultaat_list
      description: Deze lijst kan gefilterd wordt met query-string parameters.
      summary: Alle RESULTAATen van ZAAKen opvragen.
      parameters:
        - name: zaak
          required: false
          in: query
          description: URL-referentie naar de ZAAK.
          schema:
            type: string
            format: uri
        - name: resultaattype
          required: false
          in: query
          description: URL-referentie naar het RESULTAATTYPE (in de Catalogi API).
          schema:
            type: string
            format: uri
        - name: page
          required: false
          in: query
          description: Een pagina binnen de gepagineerde set resultaten.
          schema:
            type: integer
      tags:
        - resultaten
      security:
        - JWT-Claims:
            - zaken.lezen
      responses:
        "200":
          headers:
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedResultaatList"
          description: OK
        "400":
          $ref: '#/components/responses/ValidatieFoutResponse'
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
    post:
      operationId: resultaat_create
      description: |
        **Er wordt gevalideerd op:**
        - geldigheid URL naar de ZAAK
        - geldigheid URL naar het RESULTAATTYPE
      summary: Maak een RESULTAAT bij een ZAAK aan.
      parameters:
        - in: header
          name: Content-Type
          schema:
            type: string
            enum:
              - application/json
          description: Content type van de verzoekinhoud.
          required: true
        - in: header
          name: X-NLX-Logrecord-ID
          schema:
            type: string
          description: Identifier of the request, traceable throughout the network
        - in: header
          name: X-Audit-Toelichting
          schema:
            type: string
          description: Toelichting waarom een bepaald verzoek wordt gedaan
      tags:
        - resultaten
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Resultaat"
        required: true
      security:
        - JWT-Claims:
            - (zaken.bijwerken | zaken.geforceerd-bijwerken)
      responses:
        "201":
          headers:
            Location:
              $ref: '#/components/headers/LocationHeader'
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Resultaat"
          description: Created
        "400":
          $ref: '#/components/responses/ValidatieFoutResponse'
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
  /resultaten/{uuid}:
    get:
      operationId: resultaat_retrieve
      description: Een specifieke RESULTAAT opvragen.
      summary: Een specifieke RESULTAAT opvragen.
      parameters:
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
        - in: header
          name: If-None-Match
          schema:
            type: string
          description: |
            Voer een voorwaardelijk verzoek uit. Deze header moet één of meerdere ETag-waardes bevatten van resources die de consumer gecached heeft. Indien de waarde van de ETag van de huidige resource voorkomt in deze set, dan antwoordt de provider met een lege HTTP 304 request. Zie [MDN](https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/If-None-Match) voor meer informatie.
          examples:
            OneValue:
              value: '"79054025255fb1a26e4bc422aef54eb4"'
              summary: "Eén ETag-waarde"
            MultipleValues:
              value: '"79054025255fb1a26e4bc422aef54eb4", "e4d909c290d0fb1ca068ffaddf22cbd0"'
              summary: Meerdere ETag-waardes
      tags:
        - resultaten
      security:
        - JWT-Claims:
            - zaken.lezen
      responses:
        "200":
          headers:
            ETag:
              $ref: '#/components/headers/EtagHeader'
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Resultaat"
          description: OK
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
    put:
      operationId: resultaat_update
      description: |
        **Er wordt gevalideerd op** 
        - geldigheid URL naar de ZAAK
        - het RESULTAATTYPE mag niet gewijzigd worden
      summary: Werk een RESULTAAT in zijn geheel bij.
      parameters:
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
        - in: header
          name: Content-Type
          schema:
            type: string
            enum:
              - application/json
          description: Content type van de verzoekinhoud.
          required: true
        - in: header
          name: X-NLX-Logrecord-ID
          schema:
            type: string
          description: Identifier of the request, traceable throughout the network
        - in: header
          name: X-Audit-Toelichting
          schema:
            type: string
          description: Toelichting waarom een bepaald verzoek wordt gedaan
      tags:
        - resultaten
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Resultaat"
        required: true
      security:
        - JWT-Claims:
            - (zaken.bijwerken | zaken.geforceerd-bijwerken)
      responses:
        "200":
          headers:
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Resultaat"
          description: OK
        "400":
          $ref: '#/components/responses/ValidatieFoutResponse'
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
    patch:
      operationId: resultaat_partial_update
      description: |
        **Er wordt gevalideerd op** 
        - geldigheid URL naar de ZAAK
        - het RESULTAATTYPE mag niet gewijzigd worden
      summary: Werk een RESULTAAT deels bij.
      parameters:
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
        - in: header
          name: Content-Type
          schema:
            type: string
            enum:
              - application/json
          description: Content type van de verzoekinhoud.
          required: true
        - in: header
          name: X-NLX-Logrecord-ID
          schema:
            type: string
          description: Identifier of the request, traceable throughout the network
        - in: header
          name: X-Audit-Toelichting
          schema:
            type: string
          description: Toelichting waarom een bepaald verzoek wordt gedaan
      tags:
        - resultaten
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PatchedResultaat"
      security:
        - JWT-Claims:
            - (zaken.bijwerken | zaken.geforceerd-bijwerken)
      responses:
        "200":
          headers:
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Resultaat"
          description: OK
        "400":
          $ref: '#/components/responses/ValidatieFoutResponse'
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
    delete:
      operationId: resultaat_destroy
      description: Verwijder een RESULTAAT van een ZAAK.
      summary: Verwijder een RESULTAAT van een ZAAK.
      parameters:
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
        - in: header
          name: X-NLX-Logrecord-ID
          schema:
            type: string
          description: Identifier of the request, traceable throughout the network
        - in: header
          name: X-Audit-Toelichting
          schema:
            type: string
          description: Toelichting waarom een bepaald verzoek wordt gedaan
      tags:
        - resultaten
      security:
        - JWT-Claims:
            - (zaken.bijwerken | zaken.geforceerd-bijwerken)
      responses:
        "204":
          description: No content
          headers:
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
    head:
      operationId: resultaat_headers
      description: Vraag de headers op die je bij een GET request zou krijgen.
      summary: "De headers voor een specifiek(e) RESULTAAT opvragen "
      parameters:
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
        - in: header
          name: If-None-Match
          schema:
            type: string
          description: |
            Voer een voorwaardelijk verzoek uit. Deze header moet één of meerdere ETag-waardes bevatten van resources die de consumer gecached heeft. Indien de waarde van de ETag van de huidige resource voorkomt in deze set, dan antwoordt de provider met een lege HTTP 304 request. Zie [MDN](https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/If-None-Match) voor meer informatie.
          examples:
            OneValue:
              value: '"79054025255fb1a26e4bc422aef54eb4"'
              summary: "Eén ETag-waarde"
            MultipleValues:
              value: '"79054025255fb1a26e4bc422aef54eb4", "e4d909c290d0fb1ca068ffaddf22cbd0"'
              summary: Meerdere ETag-waardes
      tags:
        - resultaten
      responses:
        "200":
          headers:
            ETag:
              $ref: '#/components/headers/EtagHeader'
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          description: OK
  /rollen:
    get:
      operationId: rol_list
      description: Deze lijst kan gefilterd wordt met query-string parameters.
      summary: Alle ROLlen bij ZAAKen opvragen.
      parameters:
        - name: zaak
          required: false
          in: query
          description: URL-referentie naar de ZAAK.
          schema:
            type: string
            format: uri
        - name: betrokkene
          required: false
          in: query
          description: URL-referentie naar een betrokkene gerelateerd aan de ZAAK.
          schema:
            type: string
            format: uri
        - name: betrokkeneType
          required: false
          in: query
          description: Type van de `betrokkene`.
          schema:
            type: string
            enum:
              - natuurlijk_persoon
              - niet_natuurlijk_persoon
              - vestiging
              - organisatorische_eenheid
              - medewerker
        - name: betrokkeneIdentificatie__natuurlijkPersoon__inpBsn
          required: false
          in: query
          description: |
            Het burgerservicenummer, bedoeld in artikel 1.1 van de Wet algemene bepalingen burgerservicenummer.
          schema:
            type: string
        - name: betrokkeneIdentificatie__natuurlijkPersoon__anpIdentificatie
          required: false
          in: query
          description: Het door de gemeente uitgegeven unieke nummer voor een ANDER
            NATUURLIJK PERSOON
          schema:
            type: string
        - name: betrokkeneIdentificatie__natuurlijkPersoon__inpA_nummer
          required: false
          in: query
          description: Het administratienummer van de persoon, bedoeld in de Wet BRP
          schema:
            type: string
        - name: betrokkeneIdentificatie__nietNatuurlijkPersoon__innNnpId
          required: false
          in: query
          description: |
            Het door een kamer toegekend uniek nummer voor de INGESCHREVEN NIET-NATUURLIJK PERSOON
          schema:
            type: string
        - name: betrokkeneIdentificatie__nietNatuurlijkPersoon__annIdentificatie
          required: false
          in: query
          description: |
            Het door de gemeente uitgegeven unieke nummer voor een ANDER NIET-NATUURLIJK PERSOON
          schema:
            type: string
        - name: betrokkeneIdentificatie__vestiging__vestigingsNummer
          required: false
          in: query
          description: Een korte unieke aanduiding van de Vestiging.
          schema:
            type: string
        - name: betrokkeneIdentificatie__organisatorischeEenheid__identificatie
          required: false
          in: query
          description: Een korte identificatie van de organisatorische eenheid.
          schema:
            type: string
        - name: betrokkeneIdentificatie__medewerker__identificatie
          required: false
          in: query
          description: Een korte unieke aanduiding van de MEDEWERKER.
          schema:
            type: string
        - name: roltype
          required: false
          in: query
          description: URL-referentie naar een roltype binnen het ZAAKTYPE van de
            ZAAK.
          schema:
            type: string
            format: uri
        - name: omschrijving
          required: false
          in: query
          description: Omschrijving van de aard van de ROL, afgeleid uit het ROLTYPE.
          schema:
            type: string
        - name: omschrijvingGeneriek
          required: false
          in: query
          description: Algemeen gehanteerde benaming van de aard van de ROL, afgeleid
            uit het ROLTYPE.
          schema:
            type: string
            enum:
              - adviseur
              - behandelaar
              - belanghebbende
              - beslisser
              - initiator
              - klantcontacter
              - zaakcoordinator
              - mede_initiator
        - name: page
          required: false
          in: query
          description: Een pagina binnen de gepagineerde set resultaten.
          schema:
            type: integer
      tags:
        - rollen
      security:
        - JWT-Claims:
            - zaken.lezen
      responses:
        "200":
          headers:
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedRolList"
          description: OK
        "400":
          $ref: '#/components/responses/ValidatieFoutResponse'
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
    post:
      operationId: rol_create
      description: Maak een ROL aan bij een ZAAK.
      summary: Maak een ROL aan bij een ZAAK.
      parameters:
        - in: header
          name: Content-Type
          schema:
            type: string
            enum:
              - application/json
          description: Content type van de verzoekinhoud.
          required: true
        - in: header
          name: X-NLX-Logrecord-ID
          schema:
            type: string
          description: Identifier of the request, traceable throughout the network
        - in: header
          name: X-Audit-Toelichting
          schema:
            type: string
          description: Toelichting waarom een bepaald verzoek wordt gedaan
      tags:
        - rollen
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RolVariant"
        required: true
      security:
        - JWT-Claims:
            - (zaken.bijwerken | zaken.geforceerd-bijwerken)
      responses:
        "201":
          headers:
            Location:
              $ref: '#/components/headers/LocationHeader'
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RolVariant"
          description: Created
        "400":
          $ref: '#/components/responses/ValidatieFoutResponse'
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
  /rollen/{uuid}:
    get:
      operationId: rol_retrieve
      description: Een specifieke ROL bij een ZAAK opvragen.
      summary: Een specifieke ROL bij een ZAAK opvragen.
      parameters:
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
        - in: header
          name: If-None-Match
          schema:
            type: string
          description: |
            Voer een voorwaardelijk verzoek uit. Deze header moet één of meerdere ETag-waardes bevatten van resources die de consumer gecached heeft. Indien de waarde van de ETag van de huidige resource voorkomt in deze set, dan antwoordt de provider met een lege HTTP 304 request. Zie [MDN](https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/If-None-Match) voor meer informatie.
          examples:
            OneValue:
              value: '"79054025255fb1a26e4bc422aef54eb4"'
              summary: "Eén ETag-waarde"
            MultipleValues:
              value: '"79054025255fb1a26e4bc422aef54eb4", "e4d909c290d0fb1ca068ffaddf22cbd0"'
              summary: Meerdere ETag-waardes
      tags:
        - rollen
      security:
        - JWT-Claims:
            - zaken.lezen
      responses:
        "200":
          headers:
            ETag:
              $ref: '#/components/headers/EtagHeader'
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RolVariant"
          description: OK
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
    delete:
      operationId: rol_destroy
      description: Verwijder een ROL van een ZAAK.
      summary: Verwijder een ROL van een ZAAK.
      parameters:
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
        - in: header
          name: X-NLX-Logrecord-ID
          schema:
            type: string
          description: Identifier of the request, traceable throughout the network
        - in: header
          name: X-Audit-Toelichting
          schema:
            type: string
          description: Toelichting waarom een bepaald verzoek wordt gedaan
      tags:
        - rollen
      security:
        - JWT-Claims:
            - (zaken.bijwerken | zaken.geforceerd-bijwerken)
      responses:
        "204":
          description: No content
          headers:
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
    head:
      operationId: rol_headers
      description: Vraag de headers op die je bij een GET request zou krijgen.
      summary: "De headers voor een specifiek(e) ROL opvragen "
      parameters:
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
        - in: header
          name: If-None-Match
          schema:
            type: string
          description: |
            Voer een voorwaardelijk verzoek uit. Deze header moet één of meerdere ETag-waardes bevatten van resources die de consumer gecached heeft. Indien de waarde van de ETag van de huidige resource voorkomt in deze set, dan antwoordt de provider met een lege HTTP 304 request. Zie [MDN](https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/If-None-Match) voor meer informatie.
          examples:
            OneValue:
              value: '"79054025255fb1a26e4bc422aef54eb4"'
              summary: "Eén ETag-waarde"
            MultipleValues:
              value: '"79054025255fb1a26e4bc422aef54eb4", "e4d909c290d0fb1ca068ffaddf22cbd0"'
              summary: Meerdere ETag-waardes
      tags:
        - rollen
      responses:
        "200":
          headers:
            ETag:
              $ref: '#/components/headers/EtagHeader'
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          description: OK
  /statussen:
    get:
      operationId: status_list
      description: Deze lijst kan gefilterd wordt met query-string parameters.
      summary: Alle STATUSsen bij ZAAKen opvragen.
      parameters:
        - name: zaak
          required: false
          in: query
          description: URL-referentie naar de ZAAK.
          schema:
            type: string
            format: uri
        - name: statustype
          required: false
          in: query
          description: URL-referentie naar het STATUSTYPE (in de Catalogi API).
          schema:
            type: string
            format: uri
        - name: indicatieLaatstGezetteStatus
          required: false
          in: query
          description: |
            Het gegeven is afleidbaar uit de historie van de attribuutsoort Datum status gezet van van alle statussen bij de desbetreffende zaak.
          schema:
            type: string
        - name: page
          required: false
          in: query
          description: Een pagina binnen de gepagineerde set resultaten.
          schema:
            type: integer
      tags:
        - statussen
      security:
        - JWT-Claims:
            - zaken.lezen
      responses:
        "200":
          headers:
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedStatusList"
          description: OK
        "400":
          $ref: '#/components/responses/ValidatieFoutResponse'
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
    post:
      operationId: status_create
      description: |
        **Er wordt gevalideerd op**
        - geldigheid URL naar de ZAAK
        - geldigheid URL naar het STATUSTYPE
        - indien het de eindstatus betreft, dan moet het attribuut `indicatieGebruiksrecht` gezet zijn op alle informatieobjecten dieaan de zaak gerelateerd zijn

        **Opmerkingen**
        - Indien het statustype de eindstatus is (volgens het ZTC), dan wordt de zaak afgesloten door de einddatum te zetten.
      summary: Maak een STATUS aan voor een ZAAK.
      parameters:
        - in: header
          name: Content-Type
          schema:
            type: string
            enum:
              - application/json
          description: Content type van de verzoekinhoud.
          required: true
        - in: header
          name: X-NLX-Logrecord-ID
          schema:
            type: string
          description: Identifier of the request, traceable throughout the network
        - in: header
          name: X-Audit-Toelichting
          schema:
            type: string
          description: Toelichting waarom een bepaald verzoek wordt gedaan
      tags:
        - statussen
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Status"
        required: true
      security:
        - JWT-Claims:
            - (zaken.aanmaken | zaken.statussen.toevoegen | zaken.heropenen)
      responses:
        "201":
          headers:
            Location:
              $ref: '#/components/headers/LocationHeader'
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StatusRequestbody"
          description: Created
        "400":
          $ref: '#/components/responses/ValidatieFoutResponse'
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
  /statussen/{uuid}:
    get:
      operationId: status_retrieve
      description: Een specifieke STATUS van een ZAAK opvragen.
      summary: Een specifieke STATUS van een ZAAK opvragen.
      parameters:
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
        - in: header
          name: If-None-Match
          schema:
            type: string
          description: |
            Voer een voorwaardelijk verzoek uit. Deze header moet één of meerdere ETag-waardes bevatten van resources die de consumer gecached heeft. Indien de waarde van de ETag van de huidige resource voorkomt in deze set, dan antwoordt de provider met een lege HTTP 304 request. Zie [MDN](https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/If-None-Match) voor meer informatie.
          examples:
            OneValue:
              value: '"79054025255fb1a26e4bc422aef54eb4"'
              summary: "Eén ETag-waarde"
            MultipleValues:
              value: '"79054025255fb1a26e4bc422aef54eb4", "e4d909c290d0fb1ca068ffaddf22cbd0"'
              summary: Meerdere ETag-waardes
      tags:
        - statussen
      security:
        - JWT-Claims:
            - zaken.lezen
      responses:
        "200":
          headers:
            ETag:
              $ref: '#/components/headers/EtagHeader'
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Status"
          description: OK
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
    head:
      operationId: status_headers
      description: Vraag de headers op die je bij een GET request zou krijgen.
      summary: "De headers voor een specifiek(e) STATUS opvragen "
      parameters:
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
        - in: header
          name: If-None-Match
          schema:
            type: string
          description: |
            Voer een voorwaardelijk verzoek uit. Deze header moet één of meerdere ETag-waardes bevatten van resources die de consumer gecached heeft. Indien de waarde van de ETag van de huidige resource voorkomt in deze set, dan antwoordt de provider met een lege HTTP 304 request. Zie [MDN](https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/If-None-Match) voor meer informatie.
          examples:
            OneValue:
              value: '"79054025255fb1a26e4bc422aef54eb4"'
              summary: "Eén ETag-waarde"
            MultipleValues:
              value: '"79054025255fb1a26e4bc422aef54eb4", "e4d909c290d0fb1ca068ffaddf22cbd0"'
              summary: Meerdere ETag-waardes
      tags:
        - statussen
      responses:
        "200":
          headers:
            ETag:
              $ref: '#/components/headers/EtagHeader'
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          description: OK
  /zaakcontactmomenten:
    get:
      operationId: zaakcontactmoment_list
      description: Alle ZAAKCONTACTMOMENTen opvragen.
      summary: Alle ZAAKCONTACTMOMENTen opvragen.
      parameters:
        - name: zaak
          required: false
          in: query
          description: URL-referentie naar de ZAAK.
          schema:
            type: string
            format: uri
        - name: contactmoment
          required: false
          in: query
          description: URL-referentie naar het CONTACTMOMENT (in de Klantinteractie
            API)
          schema:
            type: string
            format: uri
      tags:
        - zaakcontactmomenten
      security:
        - JWT-Claims:
            - zaken.lezen
      responses:
        "200":
          headers:
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ZaakContactMoment"
          description: OK
        "400":
          $ref: '#/components/responses/ValidatieFoutResponse'
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
    post:
      operationId: zaakcontactmoment_create
      description: |
        **Er wordt gevalideerd op:**
        - geldigheid URL naar het CONTACTMOMENT
      summary: Maak een ZAAKCONTACTMOMENT aan.
      parameters:
        - in: header
          name: Content-Type
          schema:
            type: string
            enum:
              - application/json
          description: Content type van de verzoekinhoud.
          required: true
        - in: header
          name: X-NLX-Logrecord-ID
          schema:
            type: string
          description: Identifier of the request, traceable throughout the network
        - in: header
          name: X-Audit-Toelichting
          schema:
            type: string
          description: Toelichting waarom een bepaald verzoek wordt gedaan
      tags:
        - zaakcontactmomenten
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ZaakContactMoment"
        required: true
      security:
        - JWT-Claims:
            - zaken.bijwerken
      responses:
        "201":
          headers:
            Location:
              $ref: '#/components/headers/LocationHeader'
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ZaakContactMoment"
          description: Created
        "400":
          $ref: '#/components/responses/ValidatieFoutResponse'
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
  /zaakcontactmomenten/{uuid}:
    get:
      operationId: zaakcontactmoment_retrieve
      description: Een specifiek ZAAKCONTACTMOMENT opvragen.
      summary: "Een specifiek ZAAKCONTACTMOMENT opvragen "
      parameters:
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
      tags:
        - zaakcontactmomenten
      security:
        - JWT-Claims:
            - zaken.lezen
      responses:
        "200":
          headers:
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ZaakContactMoment"
          description: OK
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
    delete:
      operationId: zaakcontactmoment_destroy
      description: Verwijder een ZAAKCONTACTMOMENT.
      summary: Verwijder een ZAAKCONTACTMOMENT.
      parameters:
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
        - in: header
          name: X-NLX-Logrecord-ID
          schema:
            type: string
          description: Identifier of the request, traceable throughout the network
        - in: header
          name: X-Audit-Toelichting
          schema:
            type: string
          description: Toelichting waarom een bepaald verzoek wordt gedaan
      tags:
        - zaakcontactmomenten
      security:
        - JWT-Claims:
            - zaken.bijwerken
      responses:
        "204":
          description: No content
          headers:
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
  /zaakinformatieobjecten:
    get:
      operationId: zaakinformatieobject_list
      description: Deze lijst kan gefilterd wordt met query-string parameters.
      summary: "Alle ZAAK-INFORMATIEOBJECT relaties opvragen "
      parameters:
        - name: zaak
          required: false
          in: query
          description: URL-referentie naar de ZAAK.
          schema:
            type: string
            format: uri
        - name: informatieobject
          required: false
          in: query
          description: |
            URL-referentie naar het INFORMATIEOBJECT (in de Documenten API), waar ook de relatieinformatie opgevraagd kan worden.
          schema:
            type: string
            format: uri
      tags:
        - zaakinformatieobjecten
      security:
        - JWT-Claims:
            - zaken.lezen
      responses:
        "200":
          headers:
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ZaakInformatieObject"
          description: OK
        "400":
          $ref: '#/components/responses/ValidatieFoutResponse'
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
    post:
      operationId: zaakinformatieobject_create
      description: |
        Maak een ZAAK-INFORMATIEOBJECT relatie aan.

        **Er wordt gevalideerd op**
        - geldigheid zaak URL
        - geldigheid informatieobject URL
        - `zaak.archiefstatus` moet gelijk zijn aan 'nog_te_archiveren' 
        - de combinatie informatieobject en zaak moet uniek zijn

        **Opmerkingen**
        - De registratiedatum wordt door het systeem op 'NU' gezet. De `aardRelatie` wordt ook door het systeem gezet.
        - Bij het aanmaken wordt ook in de Documenten API de gespiegelde relatie aangemaakt, echter zonder de relatie-informatie.
      summary: Maak een ZAAK-INFORMATIEOBJECT relatie aan.
      parameters:
        - in: header
          name: Content-Type
          schema:
            type: string
            enum:
              - application/json
          description: Content type van de verzoekinhoud.
          required: true
        - in: header
          name: X-NLX-Logrecord-ID
          schema:
            type: string
          description: Identifier of the request, traceable throughout the network
        - in: header
          name: X-Audit-Toelichting
          schema:
            type: string
          description: Toelichting waarom een bepaald verzoek wordt gedaan
      tags:
        - zaakinformatieobjecten
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ZaakInformatieObject"
        required: true
      security:
        - JWT-Claims:
            - (zaken.aanmaken | zaken.bijwerken | zaken.geforceerd-bijwerken)
      responses:
        "201":
          headers:
            Location:
              $ref: '#/components/headers/LocationHeader'
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ZaakInformatieObject"
          description: Created
        "400":
          $ref: '#/components/responses/ValidatieFoutResponse'
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
  /zaakinformatieobjecten/{uuid}:
    get:
      operationId: zaakinformatieobject_retrieve
      description: Een specifieke ZAAK-INFORMATIEOBJECT relatie opvragen.
      summary: Een specifieke ZAAK-INFORMATIEOBJECT relatie opvragen.
      parameters:
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
        - in: header
          name: If-None-Match
          schema:
            type: string
          description: |
            Voer een voorwaardelijk verzoek uit. Deze header moet één of meerdere ETag-waardes bevatten van resources die de consumer gecached heeft. Indien de waarde van de ETag van de huidige resource voorkomt in deze set, dan antwoordt de provider met een lege HTTP 304 request. Zie [MDN](https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/If-None-Match) voor meer informatie.
          examples:
            OneValue:
              value: '"79054025255fb1a26e4bc422aef54eb4"'
              summary: "Eén ETag-waarde"
            MultipleValues:
              value: '"79054025255fb1a26e4bc422aef54eb4", "e4d909c290d0fb1ca068ffaddf22cbd0"'
              summary: Meerdere ETag-waardes
      tags:
        - zaakinformatieobjecten
      security:
        - JWT-Claims:
            - zaken.lezen
      responses:
        "200":
          headers:
            ETag:
              $ref: '#/components/headers/EtagHeader'
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ZaakInformatieObject"
          description: OK
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
    put:
      operationId: zaakinformatieobject_update
      description: |
        Je mag enkel de gegevens van de relatie bewerken, en niet de relatie zelf aanpassen.

        **Er wordt gevalideerd op** 
        - informatieobject URL en zaak URL mogen niet veranderen
      summary: Werk een ZAAK-INFORMATIEOBJECT relatie in zijn geheel bij.
      parameters:
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
        - in: header
          name: Content-Type
          schema:
            type: string
            enum:
              - application/json
          description: Content type van de verzoekinhoud.
          required: true
        - in: header
          name: X-NLX-Logrecord-ID
          schema:
            type: string
          description: Identifier of the request, traceable throughout the network
        - in: header
          name: X-Audit-Toelichting
          schema:
            type: string
          description: Toelichting waarom een bepaald verzoek wordt gedaan
      tags:
        - zaakinformatieobjecten
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ZaakInformatieObject"
        required: true
      security:
        - JWT-Claims:
            - (zaken.bijwerken | zaken.geforceerd-bijwerken)
      responses:
        "200":
          headers:
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ZaakInformatieObject"
          description: OK
        "400":
          $ref: '#/components/responses/ValidatieFoutResponse'
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
    patch:
      operationId: zaakinformatieobject_partial_update
      description: |
        Je mag enkel de gegevens van de relatie bewerken, en niet de relatie zelf aanpassen.

        **Er wordt gevalideerd op** 
        - informatieobject URL en zaak URL mogen niet veranderen
      summary: Werk een ZAAK-INFORMATIEOBJECT relatie deels bij.
      parameters:
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
        - in: header
          name: Content-Type
          schema:
            type: string
            enum:
              - application/json
          description: Content type van de verzoekinhoud.
          required: true
        - in: header
          name: X-NLX-Logrecord-ID
          schema:
            type: string
          description: Identifier of the request, traceable throughout the network
        - in: header
          name: X-Audit-Toelichting
          schema:
            type: string
          description: Toelichting waarom een bepaald verzoek wordt gedaan
      tags:
        - zaakinformatieobjecten
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PatchedZaakInformatieObject"
      security:
        - JWT-Claims:
            - (zaken.bijwerken | zaken.geforceerd-bijwerken)
      responses:
        "200":
          headers:
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ZaakInformatieObject"
          description: OK
        "400":
          $ref: '#/components/responses/ValidatieFoutResponse'
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
    delete:
      operationId: zaakinformatieobject_destroy
      description: |
        De gespiegelde relatie in de Documenten API wordt door de Zaken API verwijderd. Consumers kunnen dit niet handmatig doen.
      summary: Verwijder een ZAAK-INFORMATIEOBJECT relatie.
      parameters:
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
        - in: header
          name: X-NLX-Logrecord-ID
          schema:
            type: string
          description: Identifier of the request, traceable throughout the network
        - in: header
          name: X-Audit-Toelichting
          schema:
            type: string
          description: Toelichting waarom een bepaald verzoek wordt gedaan
      tags:
        - zaakinformatieobjecten
      security:
        - JWT-Claims:
            - (zaken.bijwerken | zaken.geforceerd-bijwerken | zaken.verwijderen)
      responses:
        "204":
          description: No content
          headers:
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
    head:
      operationId: zaakinformatieobject_headers
      description: Vraag de headers op die je bij een GET request zou krijgen.
      summary: "De headers voor een specifiek(e) ZAAKINFORMATIEOBJECT opvragen "
      parameters:
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
        - in: header
          name: If-None-Match
          schema:
            type: string
          description: |
            Voer een voorwaardelijk verzoek uit. Deze header moet één of meerdere ETag-waardes bevatten van resources die de consumer gecached heeft. Indien de waarde van de ETag van de huidige resource voorkomt in deze set, dan antwoordt de provider met een lege HTTP 304 request. Zie [MDN](https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/If-None-Match) voor meer informatie.
          examples:
            OneValue:
              value: '"79054025255fb1a26e4bc422aef54eb4"'
              summary: "Eén ETag-waarde"
            MultipleValues:
              value: '"79054025255fb1a26e4bc422aef54eb4", "e4d909c290d0fb1ca068ffaddf22cbd0"'
              summary: Meerdere ETag-waardes
      tags:
        - zaakinformatieobjecten
      responses:
        "200":
          headers:
            ETag:
              $ref: '#/components/headers/EtagHeader'
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          description: OK
  /zaakobjecten:
    get:
      operationId: zaakobject_list
      description: Deze lijst kan gefilterd wordt met query-string parameters.
      summary: Alle ZAAKOBJECTen opvragen.
      parameters:
        - name: zaak
          required: false
          in: query
          description: URL-referentie naar de ZAAK.
          schema:
            type: string
            format: uri
        - name: object
          required: false
          in: query
          description: URL-referentie naar de resource die het OBJECT beschrijft.
          schema:
            type: string
            format: uri
        - name: objectType
          required: false
          in: query
          description: |
            Beschrijft het type OBJECT gerelateerd aan de ZAAK. Als er geen passend type is, dan moet het type worden opgegeven onder `objectTypeOverige`.
          schema:
            type: string
            enum:
              - adres
              - besluit
              - buurt
              - enkelvoudig_document
              - gemeente
              - gemeentelijke_openbare_ruimte
              - huishouden
              - inrichtingselement
              - kadastrale_onroerende_zaak
              - kunstwerkdeel
              - maatschappelijke_activiteit
              - medewerker
              - natuurlijk_persoon
              - niet_natuurlijk_persoon
              - openbare_ruimte
              - organisatorische_eenheid
              - pand
              - spoorbaandeel
              - status
              - terreindeel
              - terrein_gebouwd_object
              - vestiging
              - waterdeel
              - wegdeel
              - wijk
              - woonplaats
              - woz_deelobject
              - woz_object
              - woz_waarde
              - zakelijk_recht
              - overige
        - name: page
          required: false
          in: query
          description: Een pagina binnen de gepagineerde set resultaten.
          schema:
            type: integer
      tags:
        - zaakobjecten
      security:
        - JWT-Claims:
            - zaken.lezen
      responses:
        "200":
          headers:
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedZaakObjectList"
          description: OK
        "400":
          $ref: '#/components/responses/ValidatieFoutResponse'
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
    post:
      operationId: zaakobject_create
      description: |
        Maak een ZAAKOBJECT aan.

        **Er wordt gevalideerd op**
        - Indien de `object` URL opgegeveven is, dan moet deze een geldige  response (HTTP 200) geven.
        - Indien opgegeven, dan wordt `objectIdentificatie` gevalideerd tegen de `objectType` discriminator.
      summary: Maak een ZAAKOBJECT aan.
      parameters:
        - in: header
          name: Content-Type
          schema:
            type: string
            enum:
              - application/json
          description: Content type van de verzoekinhoud.
          required: true
        - in: header
          name: X-NLX-Logrecord-ID
          schema:
            type: string
          description: Identifier of the request, traceable throughout the network
        - in: header
          name: X-Audit-Toelichting
          schema:
            type: string
          description: Toelichting waarom een bepaald verzoek wordt gedaan
      tags:
        - zaakobjecten
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ZaakObjectVariant"
        required: true
      security:
        - JWT-Claims:
            - (zaken.aanmaken | zaken.bijwerken | zaken.geforceerd-bijwerken)
      responses:
        "201":
          headers:
            Location:
              $ref: '#/components/headers/LocationHeader'
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ZaakObjectVariant"
          description: Created
        "400":
          $ref: '#/components/responses/ValidatieFoutResponse'
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
  /zaakobjecten/{uuid}:
    get:
      operationId: zaakobject_retrieve
      description: Een specifieke ZAAKOBJECT opvragen.
      summary: Een specifieke ZAAKOBJECT opvragen.
      parameters:
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
        - in: header
          name: If-None-Match
          schema:
            type: string
          description: |
            Voer een voorwaardelijk verzoek uit. Deze header moet één of meerdere ETag-waardes bevatten van resources die de consumer gecached heeft. Indien de waarde van de ETag van de huidige resource voorkomt in deze set, dan antwoordt de provider met een lege HTTP 304 request. Zie [MDN](https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/If-None-Match) voor meer informatie.
          examples:
            OneValue:
              value: '"79054025255fb1a26e4bc422aef54eb4"'
              summary: "Eén ETag-waarde"
            MultipleValues:
              value: '"79054025255fb1a26e4bc422aef54eb4", "e4d909c290d0fb1ca068ffaddf22cbd0"'
              summary: Meerdere ETag-waardes
      tags:
        - zaakobjecten
      security:
        - JWT-Claims:
            - zaken.lezen
      responses:
        "200":
          headers:
            ETag:
              $ref: '#/components/headers/EtagHeader'
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ZaakObjectVariant"
          description: OK
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
    put:
      operationId: zaakobject_update
      description: |
        **Er wordt gevalideerd op** 
        - De attributen `zaak`, `object` en `objectType` mogen niet gewijzigd worden.
        - Indien opgegeven, dan wordt `objectIdentificatie` gevalideerd tegen de objectType discriminator.
      summary: Werk een ZAAKOBJECT zijn geheel bij.
      parameters:
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
        - in: header
          name: Content-Type
          schema:
            type: string
            enum:
              - application/json
          description: Content type van de verzoekinhoud.
          required: true
      tags:
        - zaakobjecten
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ZaakObjectVariant"
        required: true
      security:
        - JWT-Claims:
            - (zaken.bijwerken | zaken.geforceerd-bijwerken)
      responses:
        "200":
          headers:
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ZaakObjectVariant"
          description: OK
        "400":
          $ref: '#/components/responses/ValidatieFoutResponse'
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
    patch:
      operationId: zaakobject_partial_update
      description: |
        **Er wordt gevalideerd op** 
        - De attributen `zaak`, `object` en `objectType` mogen niet gewijzigd worden.
        - Indien opgegeven, dan wordt `objectIdentificatie` gevalideerd tegen de objectType discriminator.
      summary: Werk een ZAAKOBJECT deels bij.
      parameters:
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
        - in: header
          name: Content-Type
          schema:
            type: string
            enum:
              - application/json
          description: Content type van de verzoekinhoud.
          required: true
      tags:
        - zaakobjecten
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PatchedZaakObjectVariant"
      security:
        - JWT-Claims:
            - (zaken.bijwerken | zaken.geforceerd-bijwerken)
      responses:
        "200":
          headers:
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ZaakObjectVariant"
          description: OK
        "400":
          $ref: '#/components/responses/ValidatieFoutResponse'
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
    delete:
      operationId: zaakobject_destroy
      description: |
        Verbreek de relatie tussen een ZAAK en een OBJECT door de ZAAKOBJECT resource te verwijderen.
      summary: Verwijder een ZAAKOBJECT.
      parameters:
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
      tags:
        - zaakobjecten
      security:
        - JWT-Claims:
            - (zaken.bijwerken | zaken.geforceerd-bijwerken | zaken.verwijderen)
      responses:
        "204":
          description: No content
          headers:
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
    head:
      operationId: zaakobject_headers
      description: Vraag de headers op die je bij een GET request zou krijgen.
      summary: "De headers voor een specifiek(e) ZAAKOBJECT opvragen "
      parameters:
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
        - in: header
          name: If-None-Match
          schema:
            type: string
          description: |
            Voer een voorwaardelijk verzoek uit. Deze header moet één of meerdere ETag-waardes bevatten van resources die de consumer gecached heeft. Indien de waarde van de ETag van de huidige resource voorkomt in deze set, dan antwoordt de provider met een lege HTTP 304 request. Zie [MDN](https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/If-None-Match) voor meer informatie.
          examples:
            OneValue:
              value: '"79054025255fb1a26e4bc422aef54eb4"'
              summary: "Eén ETag-waarde"
            MultipleValues:
              value: '"79054025255fb1a26e4bc422aef54eb4", "e4d909c290d0fb1ca068ffaddf22cbd0"'
              summary: Meerdere ETag-waardes
      tags:
        - zaakobjecten
      responses:
        "200":
          headers:
            ETag:
              $ref: '#/components/headers/EtagHeader'
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          description: OK
  /zaakverzoeken:
    get:
      operationId: zaakverzoek_list
      description: Alle ZAAK-VERZOEKen opvragen.
      summary: Alle ZAAK-VERZOEKen opvragen.
      parameters:
        - name: zaak
          required: false
          in: query
          description: URL-referentie naar de ZAAK.
          schema:
            type: string
            format: uri
        - name: verzoek
          required: false
          in: query
          description: URL-referentie naar het VERZOEK (in de Klantinteractie API)
          schema:
            type: string
            format: uri
      tags:
        - zaakverzoeken
      security:
        - JWT-Claims:
            - zaken.lezen
      responses:
        "200":
          headers:
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ZaakVerzoek"
          description: OK
        "400":
          $ref: '#/components/responses/ValidatieFoutResponse'
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
    post:
      operationId: zaakverzoek_create
      description: |
        **Er wordt gevalideerd op**
        - geldigheid URL naar de VERZOEK
      summary: Maak een ZAAK-VERZOEK aan.
      parameters:
        - in: header
          name: Content-Type
          schema:
            type: string
            enum:
              - application/json
          description: Content type van de verzoekinhoud.
          required: true
        - in: header
          name: X-NLX-Logrecord-ID
          schema:
            type: string
          description: Identifier of the request, traceable throughout the network
        - in: header
          name: X-Audit-Toelichting
          schema:
            type: string
          description: Toelichting waarom een bepaald verzoek wordt gedaan
      tags:
        - zaakverzoeken
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ZaakVerzoek"
        required: true
      security:
        - JWT-Claims:
            - zaken.bijwerken
      responses:
        "201":
          headers:
            Location:
              $ref: '#/components/headers/LocationHeader'
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ZaakVerzoek"
          description: Created
        "400":
          $ref: '#/components/responses/ValidatieFoutResponse'
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
  /zaakverzoeken/{uuid}:
    get:
      operationId: zaakverzoek_retrieve
      description: Een specifieke ZAAK-VERZOEK opvragen.
      summary: Een specifieke ZAAK-VERZOEK opvragen.
      parameters:
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
      tags:
        - zaakverzoeken
      security:
        - JWT-Claims:
            - zaken.lezen
      responses:
        "200":
          headers:
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ZaakVerzoek"
          description: OK
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
    delete:
      operationId: zaakverzoek_destroy
      description: Verwijder een ZAAK-VERZOEK.
      summary: Verwijder een ZAAK-VERZOEK.
      parameters:
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
        - in: header
          name: X-NLX-Logrecord-ID
          schema:
            type: string
          description: Identifier of the request, traceable throughout the network
        - in: header
          name: X-Audit-Toelichting
          schema:
            type: string
          description: Toelichting waarom een bepaald verzoek wordt gedaan
      tags:
        - zaakverzoeken
      security:
        - JWT-Claims:
            - zaken.bijwerken
      responses:
        "204":
          description: No content
          headers:
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
  /zaken:
    get:
      operationId: zaak_list
      description: Deze lijst kan gefilterd wordt met query-string parameters.
      summary: Alle ZAAKen opvragen.
      parameters:
        - name: identificatie
          required: false
          in: query
          description: |
            De unieke identificatie van de ZAAK binnen de organisatie die verantwoordelijk is voor de behandeling van de ZAAK.
          schema:
            type: string
            example: "zaak123"
        - name: bronorganisatie
          required: false
          in: query
          description: |
            Het RSIN van de Niet-natuurlijk persoon zijnde de organisatie die de zaak heeft gecreeerd. Dit moet een geldig RSIN zijn van 9 nummers en voldoen aan https://nl.wikipedia.org/wiki/Burgerservicenummer#11-proef
          schema:
            type: string
            example: "000000000"
        - name: bronorganisatie__in
          required: false
          in: query
          description: Multiple values may be separated by commas.
          schema:
            type: array
            items:
              type: string
            example:
              - "000000000"
              - "479559740"
              - "875883461"
          style: form
          explode: false
        - name: zaaktype
          required: false
          in: query
          description: |
            URL-referentie naar het ZAAKTYPE (in de Catalogi API) in de CATALOGUS waar deze voorkomt
          schema:
            type: string
            format: uri
            example: "https://catalogi-api.test.vng.cloud/api/v1/zaaktypen/e80b7507-199a-484c-ad49-c41a1e43a6e7"
        - name: archiefnominatie
          required: false
          in: query
          description: |
            Aanduiding of het zaakdossier blijvend bewaard of na een bepaalde termijn vernietigd moet worden.
          schema:
            type: string
            enum:
              - blijvend_bewaren
              - vernietigen
            example: "blijvend_bewaren"
        - name: archiefnominatie__in
          required: false
          in: query
          description: Multiple values may be separated by commas.
          schema:
            type: array
            items:
              type: string
            example:
              - "blijvend_bewaren"
              - "vernietigen"
          style: form
          explode: false
        - name: archiefactiedatum
          required: false
          in: query
          description: |
            De datum waarop het gearchiveerde zaakdossier vernietigd moet worden dan wel overgebracht moet worden naar een archiefbewaarplaats. Wordt automatisch berekend bij het aanmaken of wijzigen van een RESULTAAT aan deze ZAAK indien nog leeg.
          schema:
            type: string
            example: "2019-01-02"
        - name: archiefactiedatum__isnull
          required: false
          in: query
          description: De archiefactiedatum is leeg.
          schema:
            type: boolean
            example: false
        - name: archiefactiedatum__lt
          required: false
          in: query
          description: De archiefactiedatum is kleiner dan de opgegeven datum.
          schema:
            type: string
            example: "2020-01-02"
        - name: archiefactiedatum__gt
          required: false
          in: query
          description: De archiefactiedatum is groter dan de opgegeven datum.
          schema:
            type: string
            example: "2018-01-02"
        - name: archiefstatus
          required: false
          in: query
          description: |
            Aanduiding of het zaakdossier blijvend bewaard of na een bepaalde termijn vernietigd moet worden.
          schema:
            type: string
            enum:
              - nog_te_archiveren
              - gearchiveerd
              - gearchiveerd_procestermijn_onbekend
              - overgedragen
            example: "nog_te_archiveren"
        - name: archiefstatus__in
          required: false
          in: query
          description: Multiple values may be separated by commas.
          schema:
            type: array
            items:
              type: string
            example:
              - "nog_te_archiveren"
              - "gearchiveerd"
              - "gearchiveerd_procestermijn_onbekend"
              - "overgedragen"
          style: form
          explode: false
        - name: startdatum
          required: false
          in: query
          description: De datum waarop met de uitvoering van de zaak is gestart
          schema:
            type: string
            example: "2017-01-02"
        - name: startdatum__gt
          required: false
          in: query
          description: De startdatum is groter dan de opgegeven datum.
          schema:
            type: string
            example: "2018-01-02"
        - name: startdatum__gte
          required: false
          in: query
          description: De startdatum is groter dan of gelijk dan de opgegeven datum.
          schema:
            type: string
            example: "2016-01-02"
        - name: startdatum__lt
          required: false
          in: query
          description: De startdatum is kleiner dan de opgegeven datum.
          schema:
            type: string
            example: "2018-01-02"
        - name: startdatum__lte
          required: false
          in: query
          description: De startdatum is kleiner of gelijk dan de opgegeven datum.
          schema:
            type: string
            example: "2018-01-02"
        - name: registratiedatum
          required: false
          in: query
          description: |
            De datum waarop de zaakbehandelende organisatie de ZAAK heeft geregistreerd. Indien deze niet opgegeven wordt, wordt de datum van vandaag gebruikt.
          schema:
            type: string
            example: "2015-01-02"
        - name: registratiedatum__gt
          required: false
          in: query
          description: De registratiedatum is groter dan de opgegeven datum.
          schema:
            type: string
            example: "2014-01-02"
        - name: registratiedatum__lt
          required: false
          in: query
          description: De registratiedatum is kleiner dan de opgegeven datum.
          schema:
            type: string
            example: "2016-01-02"
        - name: einddatum
          required: false
          in: query
          description: De datum waarop de uitvoering van de zaak afgerond is.
          schema:
            type: string
            example: "2022-01-02"
        - name: einddatum__isnull
          required: false
          in: query
          description: De einddatum is leeg.
          schema:
            type: boolean
            example: false
        - name: einddatum__gt
          required: false
          in: query
          description: De einddatum is groter dan de opgegeven datum.
          schema:
            type: string
            example: "2021-01-02"
        - name: einddatum__lt
          required: false
          in: query
          description: De einddatum is kleiner dan de opgegeven datum.
          schema:
            type: string
            example: "2023-01-02"
        - name: einddatumGepland
          required: false
          in: query
          description: De datum waarop volgens de planning verwacht wordt dat de zaak
            afgerond wordt.
          schema:
            type: string
            example: "2022-01-02"
        - name: einddatumGepland__gt
          required: false
          in: query
          description: De geplande einddatum is groter dan de opgegeven datum.
          schema:
            type: string
            example: "2021-01-02"
        - name: einddatumGepland__lt
          required: false
          in: query
          description: De geplande einddatum is kleiner dan de opgegeven datum.
          schema:
            type: string
            example: "2024-01-02"
        - name: uiterlijkeEinddatumAfdoening
          required: false
          in: query
          description: |
            De laatste datum waarop volgens wet- en regelgeving de zaak afgerond dient te zijn.
          schema:
            type: string
            example: "2024-01-02"
        - name: uiterlijkeEinddatumAfdoening__gt
          required: false
          in: query
          description: De uiterlijke einddatumafdoening is groter dan de opgegeven
            datum.
          schema:
            type: string
            example: "2023-01-02"
        - name: uiterlijkeEinddatumAfdoening__lt
          required: false
          in: query
          description: De uiterlijke einddatumafdoening is kleiner dan de opgegeven
            datum.
          schema:
            type: string
            example: "2025-01-02"
        - name: rol__betrokkeneType
          required: false
          in: query
          description: Type van de `betrokkene`.
          schema:
            type: string
            enum:
              - natuurlijk_persoon
              - niet_natuurlijk_persoon
              - vestiging
              - organisatorische_eenheid
              - medewerker
            example: "natuurlijk_persoon"
        - name: rol__betrokkene
          required: false
          in: query
          description: URL-referentie naar een betrokkene gerelateerd aan de ZAAK.
          schema:
            type: string
            format: uri
            example: "https://example.com"
        - name: rol__omschrijvingGeneriek
          required: false
          in: query
          description: Algemeen gehanteerde benaming van de aard van de ROL, afgeleid
            uit het ROLTYPE.
          schema:
            type: string
            enum:
              - adviseur
              - behandelaar
              - belanghebbende
              - beslisser
              - initiator
              - klantcontacter
              - zaakcoordinator
              - mede_initiator
            example: "initiator"
        - name: maximaleVertrouwelijkheidaanduiding
          required: false
          in: query
          description: |
            Zaken met een vertrouwelijkheidaanduiding die beperkter is dan de aangegeven aanduiding worden uit de resultaten gefiltered.
          schema:
            type: string
            enum:
              - openbaar
              - beperkt_openbaar
              - intern
              - zaakvertrouwelijk
              - vertrouwelijk
              - confidentieel
              - geheim
              - zeer_geheim
            example: "zaakvertrouwelijk"
        - name: rol__betrokkeneIdentificatie__natuurlijkPersoon__inpBsn
          required: false
          in: query
          description: |
            Het burgerservicenummer, bedoeld in artikel 1.1 van de Wet algemene bepalingen burgerservicenummer.
          schema:
            type: string
            maxLength: 9
            example: "123456789"
        - name: rol__betrokkeneIdentificatie__natuurlijkPersoon__anpIdentificatie
          required: false
          in: query
          description: Het door de gemeente uitgegeven unieke nummer voor een ANDER
            NATUURLIJK PERSOON
          schema:
            type: string
            maxLength: 17
            example: "123456789"
        - name: rol__betrokkeneIdentificatie__natuurlijkPersoon__inpA_nummer
          required: false
          in: query
          description: Het administratienummer van de persoon, bedoeld in de Wet BRP
          schema:
            type: string
            maxLength: 10
            example: "123456789"
        - name: rol__betrokkeneIdentificatie__nietNatuurlijkPersoon__innNnpId
          required: false
          in: query
          description: |
            Het door een kamer toegekend uniek nummer voor de INGESCHREVEN NIET-NATUURLIJK PERSOON
          schema:
            type: string
            example: "123456789"
        - name: rol__betrokkeneIdentificatie__nietNatuurlijkPersoon__annIdentificatie
          required: false
          in: query
          description: |
            Het door de gemeente uitgegeven unieke nummer voor een ANDER NIET-NATUURLIJK PERSOON
          schema:
            type: string
            maxLength: 17
            example: "123456789"
        - name: rol__betrokkeneIdentificatie__vestiging__vestigingsNummer
          required: false
          in: query
          description: Een korte unieke aanduiding van de Vestiging.
          schema:
            type: string
            maxLength: 24
            example: "123456789"
        - name: rol__betrokkeneIdentificatie__medewerker__identificatie
          required: false
          in: query
          description: Een korte unieke aanduiding van de MEDEWERKER.
          schema:
            type: string
            maxLength: 24
            example: "123456789"
        - name: rol__betrokkeneIdentificatie__organisatorischeEenheid__identificatie
          required: false
          in: query
          description: Een korte identificatie van de organisatorische eenheid.
          schema:
            type: string
            example: "123456789"
        - name: expand
          in: query
          description: |
            Haal details van gelinkte resources direct op. Als je meerdere resources tegelijk wilt ophalen kun je deze scheiden met een komma. Voor het ophalen van resources die een laag dieper genest zijn wordt de punt-notatie gebruikt.
          schema:
            type: string
            example: zaaktype, status, status.statustype, hoofdzaak.status.statustype,
              hoofdzaak.deelzaken.status.statustype
        - name: ordering
          required: false
          in: query
          description: |
            Het veld waarop de resultaten geordend worden. Het minnetje betekent omgekeerde volgorde.
          schema:
            type: array
            items:
              type: string
              enum:
                - startdatum
                - -startdatum
                - einddatum
                - -einddatum
                - publicatiedatum
                - -publicatiedatum
                - archiefactiedatum
                - -archiefactiedatum
                - registratiedatum
                - -registratiedatum
                - identificatie
                - -identificatie
              example: "-startdatum"
          style: form
          explode: false
        - name: page
          required: false
          in: query
          description: Een pagina binnen de gepagineerde set resultaten.
          schema:
            type: integer
            example: 1
        - in: header
          name: Accept-Crs
          schema:
            type: string
            enum:
              - EPSG:4326
          description: |
            Het gewenste 'Coordinate Reference System' (CRS) van de geometrie in het antwoord (response body). Volgens de GeoJSON spec is WGS84 de default (EPSG:4326 is hetzelfde als WGS84).
          required: true
        - in: header
          name: Content-Crs
          schema:
            type: string
            enum:
              - EPSG:4326
          description: |
            Het 'Coordinate Reference System' (CRS) van de geometrie in de vraag (request body). Volgens de GeoJSON spec is WGS84 de default (EPSG:4326 is hetzelfde als WGS84).
          required: true
      tags:
        - zaken
      security:
        - JWT-Claims:
            - zaken.lezen
      responses:
        "200":
          headers:
            Content-Crs:
              $ref: '#/components/headers/ContentCrsHeader'
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedZaakList"
          description: OK
        "400":
          $ref: '#/components/responses/ValidatieFoutResponse'
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
    post:
      operationId: zaak_create
      description: |
        Indien geen identificatie gegeven is, dan wordt deze automatisch gegenereerd. De identificatie moet uniek zijn binnen de bronorganisatie.

        **Er wordt gevalideerd op:**
        - geldigheid `zaaktype` URL - de resource moet opgevraagd kunnen worden uit de Catalogi API en de vorm van een ZAAKTYPE hebben.
        - `zaaktype` is geen concept (`zaaktype.concept = False`)
        - `laatsteBetaaldatum` mag niet in de toekomst liggen.
        - `laatsteBetaaldatum` mag niet gezet worden als de betalingsindicatie "nvt" is.
        - `barchiefnominatie` moet een waarde hebben indien archiefstatus niet de waarde "nog_te_archiveren" heeft.
        - `archiefactiedatum` moet een waarde hebben indien archiefstatus niet de waarde "nog_te_archiveren" heeft.
        - `archiefstatus` kan alleen een waarde anders dan "nog_te_archiveren" hebben indien van alle gerelateeerde INFORMATIEOBJECTen het attribuut `status` de waarde "gearchiveerd" heeft.
      summary: Maak een ZAAK aan.
      parameters:
        - in: header
          name: Content-Type
          schema:
            type: string
            enum:
              - application/json
          description: Content type van de verzoekinhoud.
          required: true
        - in: header
          name: X-NLX-Logrecord-ID
          schema:
            type: string
          description: Identifier of the request, traceable throughout the network
        - in: header
          name: X-Audit-Toelichting
          schema:
            type: string
          description: Toelichting waarom een bepaald verzoek wordt gedaan
        - in: header
          name: Accept-Crs
          schema:
            type: string
            enum:
              - EPSG:4326
          description: |
            Het gewenste 'Coordinate Reference System' (CRS) van de geometrie in het antwoord (response body). Volgens de GeoJSON spec is WGS84 de default (EPSG:4326 is hetzelfde als WGS84).
          required: true
        - in: header
          name: Content-Crs
          schema:
            type: string
            enum:
              - EPSG:4326
          description: |
            Het 'Coordinate Reference System' (CRS) van de geometrie in de vraag (request body). Volgens de GeoJSON spec is WGS84 de default (EPSG:4326 is hetzelfde als WGS84).
          required: true
      tags:
        - zaken
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Zaak"
        required: true
      security:
        - JWT-Claims:
            - zaken.aanmaken
      responses:
        "201":
          headers:
            Content-Crs:
              $ref: '#/components/headers/ContentCrsHeader'
            Location:
              $ref: '#/components/headers/LocationHeader'
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Zaak"
          description: Created
        "400":
          $ref: '#/components/responses/ValidatieFoutResponse'
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
  /zaken/{uuid}:
    get:
      operationId: zaak_retrieve
      description: Een specifieke ZAAK opvragen.
      summary: Een specifieke ZAAK opvragen.
      parameters:
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
        - in: header
          name: If-None-Match
          schema:
            type: string
          description: |
            Voer een voorwaardelijk verzoek uit. Deze header moet één of meerdere ETag-waardes bevatten van resources die de consumer gecached heeft. Indien de waarde van de ETag van de huidige resource voorkomt in deze set, dan antwoordt de provider met een lege HTTP 304 request. Zie [MDN](https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/If-None-Match) voor meer informatie.
          examples:
            OneValue:
              value: '"79054025255fb1a26e4bc422aef54eb4"'
              summary: "Eén ETag-waarde"
            MultipleValues:
              value: '"79054025255fb1a26e4bc422aef54eb4", "e4d909c290d0fb1ca068ffaddf22cbd0"'
              summary: Meerdere ETag-waardes
        - in: header
          name: Accept-Crs
          schema:
            type: string
            enum:
              - EPSG:4326
          description: |
            Het gewenste 'Coordinate Reference System' (CRS) van de geometrie in het antwoord (response body). Volgens de GeoJSON spec is WGS84 de default (EPSG:4326 is hetzelfde als WGS84).
          required: true
        - in: header
          name: Content-Crs
          schema:
            type: string
            enum:
              - EPSG:4326
          description: |
            Het 'Coordinate Reference System' (CRS) van de geometrie in de vraag (request body). Volgens de GeoJSON spec is WGS84 de default (EPSG:4326 is hetzelfde als WGS84).
          required: true
        - name: expand
          in: query
          description: |
            Haal details van gelinkte resources direct op. Als je meerdere resources tegelijk wilt ophalen kun je deze scheiden met een komma. Voor het ophalen van resources die een laag dieper genest zijn wordt de punt-notatie gebruikt.
          schema:
            type: string
            example: zaaktype, status, status.statustype, hoofdzaak.status.statustype,
              hoofdzaak.deelzaken.status.statustype
      tags:
        - zaken
      security:
        - JWT-Claims:
            - zaken.lezen
      responses:
        "200":
          headers:
            ETag:
              $ref: '#/components/headers/EtagHeader'
            Content-Crs:
              $ref: '#/components/headers/ContentCrsHeader'
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ZaakExpanded"
          description: OK
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
    put:
      operationId: zaak_update
      description: |
        **Er wordt gevalideerd op** 
        - `identificatie` mag niet gewijzigd worden.
        - `laatsteBetaaldatum` mag niet in de toekomst liggen.
        - `laatsteBetaaldatum` mag niet gezet worden als de betalingsindicatie
        "nvt" is.
        - `archiefnominatie` moet een waarde hebben indien `archiefstatus` niet de
        waarde "nog_te_archiveren" heeft.
        - `archiefactiedatum` moet een waarde hebben indien `archiefstatus` niet de
         waarde "nog_te_archiveren" heeft.
        - `archiefstatus` kan alleen een waarde anders dan "nog_te_archiveren"
         hebben indien van alle gerelateeerde INFORMATIEOBJECTen het attribuut
          `status` de waarde "gearchiveerd" heeft.
        **Opmerkingen**
        - er worden enkel zaken getoond van de zaaktypes waar u toe geautoriseerd
         bent.
        - indien een zaak heropend moet worden, doe dit dan door een nieuwe status
         toe te voegen die NIET de eindstatus is. Zie de `Status` resource.
      summary: Werk een ZAAK in zijn geheel bij.
      parameters:
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
        - in: header
          name: Content-Type
          schema:
            type: string
            enum:
              - application/json
          description: Content type van de verzoekinhoud.
          required: true
        - in: header
          name: X-NLX-Logrecord-ID
          schema:
            type: string
          description: Identifier of the request, traceable throughout the network
        - in: header
          name: X-Audit-Toelichting
          schema:
            type: string
          description: Toelichting waarom een bepaald verzoek wordt gedaan
        - in: header
          name: Accept-Crs
          schema:
            type: string
            enum:
              - EPSG:4326
          description: |
            Het gewenste 'Coordinate Reference System' (CRS) van de geometrie in het antwoord (response body). Volgens de GeoJSON spec is WGS84 de default (EPSG:4326 is hetzelfde als WGS84).
          required: true
        - in: header
          name: Content-Crs
          schema:
            type: string
            enum:
              - EPSG:4326
          description: |
            Het 'Coordinate Reference System' (CRS) van de geometrie in de vraag (request body). Volgens de GeoJSON spec is WGS84 de default (EPSG:4326 is hetzelfde als WGS84).
          required: true
      tags:
        - zaken
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Zaak"
        required: true
      security:
        - JWT-Claims:
            - (zaken.bijwerken | zaken.geforceerd-bijwerken)
      responses:
        "200":
          headers:
            Content-Crs:
              $ref: '#/components/headers/ContentCrsHeader'
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Zaak"
          description: OK
        "400":
          $ref: '#/components/responses/ValidatieFoutResponse'
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
    patch:
      operationId: zaak_partial_update
      description: |
        **Er wordt gevalideerd op** 
        - `identificatie` mag niet gewijzigd worden.
        - `laatsteBetaaldatum` mag niet in de toekomst liggen.
        - `laatsteBetaaldatum` mag niet gezet worden als de betalingsindicatie
        "nvt" is.
        - `archiefnominatie` moet een waarde hebben indien `archiefstatus` niet de
        waarde "nog_te_archiveren" heeft.
        - `archiefactiedatum` moet een waarde hebben indien `archiefstatus` niet de
         waarde "nog_te_archiveren" heeft.
        - `archiefstatus` kan alleen een waarde anders dan "nog_te_archiveren"
         hebben indien van alle gerelateeerde INFORMATIEOBJECTen het attribuut
          `status` de waarde "gearchiveerd" heeft.
        **Opmerkingen**
        - er worden enkel zaken getoond van de zaaktypes waar u toe geautoriseerd
         bent.
        - indien een zaak heropend moet worden, doe dit dan door een nieuwe status
         toe te voegen die NIET de eindstatus is. Zie de `Status` resource.
      summary: Werk een ZAAK deels bij.
      parameters:
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
        - in: header
          name: Content-Type
          schema:
            type: string
            enum:
              - application/json
          description: Content type van de verzoekinhoud.
          required: true
        - in: header
          name: X-NLX-Logrecord-ID
          schema:
            type: string
          description: Identifier of the request, traceable throughout the network
        - in: header
          name: X-Audit-Toelichting
          schema:
            type: string
          description: Toelichting waarom een bepaald verzoek wordt gedaan
        - in: header
          name: Accept-Crs
          schema:
            type: string
            enum:
              - EPSG:4326
          description: |
            Het gewenste 'Coordinate Reference System' (CRS) van de geometrie in het antwoord (response body). Volgens de GeoJSON spec is WGS84 de default (EPSG:4326 is hetzelfde als WGS84).
          required: true
        - in: header
          name: Content-Crs
          schema:
            type: string
            enum:
              - EPSG:4326
          description: |
            Het 'Coordinate Reference System' (CRS) van de geometrie in de vraag (request body). Volgens de GeoJSON spec is WGS84 de default (EPSG:4326 is hetzelfde als WGS84).
          required: true
      tags:
        - zaken
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PatchedZaak"
      security:
        - JWT-Claims:
            - (zaken.bijwerken | zaken.geforceerd-bijwerken)
      responses:
        "200":
          headers:
            Content-Crs:
              $ref: '#/components/headers/ContentCrsHeader'
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Zaak"
          description: OK
        "400":
          $ref: '#/components/responses/ValidatieFoutResponse'
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
    delete:
      operationId: zaak_destroy
      description: |
        ***De gerelateerde resources zijn hierbij***
        - `zaak` - de deelzaken van de verwijderde hoofzaak
        - `status` - alle statussen van de verwijderde zaak
        - `resultaat` - het resultaat van de verwijderde zaak
        - `rol` - alle rollen bij de zaak
        - `zaakobject` - alle zaakobjecten bij de zaak
        - `zaakeigenschap` - alle eigenschappen van de zaak
        - `zaakkenmerk` - alle kenmerken van de zaak
        - `zaakinformatieobject` - dit moet door-cascaden naar de Documenten API, zie ook: https://github.com/VNG-Realisatie/gemma-zaken/issues/791 (TODO)
        - `klantcontact` - alle klantcontacten bij een zaak
      summary: Verwijder een ZAAK.
      parameters:
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
        - in: header
          name: X-NLX-Logrecord-ID
          schema:
            type: string
          description: Identifier of the request, traceable throughout the network
        - in: header
          name: X-Audit-Toelichting
          schema:
            type: string
          description: Toelichting waarom een bepaald verzoek wordt gedaan
        - in: header
          name: Accept-Crs
          schema:
            type: string
            enum:
              - EPSG:4326
          description: |
            Het gewenste 'Coordinate Reference System' (CRS) van de geometrie in het antwoord (response body). Volgens de GeoJSON spec is WGS84 de default (EPSG:4326 is hetzelfde als WGS84).
          required: true
        - in: header
          name: Content-Crs
          schema:
            type: string
            enum:
              - EPSG:4326
          description: |
            Het 'Coordinate Reference System' (CRS) van de geometrie in de vraag (request body). Volgens de GeoJSON spec is WGS84 de default (EPSG:4326 is hetzelfde als WGS84).
          required: true
      tags:
        - zaken
      security:
        - JWT-Claims:
            - zaken.verwijderen
      responses:
        "204":
          description: No content
          headers:
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
    head:
      operationId: zaak_headers
      description: Vraag de headers op die je bij een GET request zou krijgen.
      summary: "De headers voor een specifiek(e) ZAAK opvragen "
      parameters:
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
        - in: header
          name: If-None-Match
          schema:
            type: string
          description: |
            Voer een voorwaardelijk verzoek uit. Deze header moet één of meerdere ETag-waardes bevatten van resources die de consumer gecached heeft. Indien de waarde van de ETag van de huidige resource voorkomt in deze set, dan antwoordt de provider met een lege HTTP 304 request. Zie [MDN](https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/If-None-Match) voor meer informatie.
          examples:
            OneValue:
              value: '"79054025255fb1a26e4bc422aef54eb4"'
              summary: "Eén ETag-waarde"
            MultipleValues:
              value: '"79054025255fb1a26e4bc422aef54eb4", "e4d909c290d0fb1ca068ffaddf22cbd0"'
              summary: Meerdere ETag-waardes
        - in: header
          name: Accept-Crs
          schema:
            type: string
            enum:
              - EPSG:4326
          description: |
            Het gewenste 'Coordinate Reference System' (CRS) van de geometrie in het antwoord (response body). Volgens de GeoJSON spec is WGS84 de default (EPSG:4326 is hetzelfde als WGS84).
          required: true
        - in: header
          name: Content-Crs
          schema:
            type: string
            enum:
              - EPSG:4326
          description: |
            Het 'Coordinate Reference System' (CRS) van de geometrie in de vraag (request body). Volgens de GeoJSON spec is WGS84 de default (EPSG:4326 is hetzelfde als WGS84).
          required: true
      tags:
        - zaken
      responses:
        "200":
          headers:
            ETag:
              $ref: '#/components/headers/EtagHeader'
            Content-Crs:
              $ref: '#/components/headers/ContentCrsHeader'
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          description: OK
  /zaken/{zaak_uuid}/audittrail:
    get:
      operationId: audittrail_list
      description: Alle audit trail regels behorend bij de ZAAK.
      summary: Alle audit trail regels behorend bij de ZAAK.
      parameters:
        - in: path
          name: zaak_uuid
          schema:
            type: string
            format: uuid
          description: Unieke resource identifier (UUID4)
          required: true
      tags:
        - zaken
      security:
        - JWT-Claims:
            - audittrails.lezen
      responses:
        "200":
          headers:
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AuditTrail"
          description: OK
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
  /zaken/{zaak_uuid}/audittrail/{uuid}:
    get:
      operationId: audittrail_retrieve
      description: Een specifieke audit trail regel opvragen.
      summary: "Een specifieke audit trail regel opvragen "
      parameters:
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke identificatie van de audit regel.
          required: true
        - in: path
          name: zaak_uuid
          schema:
            type: string
            format: uuid
          description: Unieke resource identifier (UUID4)
          required: true
      tags:
        - zaken
      security:
        - JWT-Claims:
            - audittrails.lezen
      responses:
        "200":
          headers:
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuditTrail"
          description: OK
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
  /zaken/{zaak_uuid}/besluiten:
    get:
      operationId: zaakbesluit_list
      description: Alle ZAAKBESLUITen opvragen.
      summary: Alle ZAAKBESLUITen opvragen.
      parameters:
        - in: path
          name: zaak_uuid
          schema:
            type: string
            format: uuid
          description: Unieke resource identifier (UUID4)
          required: true
      tags:
        - zaken
      security:
        - JWT-Claims:
            - zaken.lezen
      responses:
        "200":
          headers:
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ZaakBesluit"
          description: OK
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
    post:
      operationId: zaakbesluit_create
      description: |
        **LET OP: Dit endpoint hoor je als consumer niet zelf aan te spreken.**

        De Besluiten API gebruikt dit endpoint om relaties te synchroniseren, daarom is dit endpoint in de Zaken API geimplementeerd.

        **Er wordt gevalideerd op:**
        - geldigheid URL naar de ZAAK
      summary: Maak een ZAAKBESLUIT aan.
      parameters:
        - in: header
          name: Content-Type
          schema:
            type: string
            enum:
              - application/json
          description: Content type van de verzoekinhoud.
          required: true
        - in: header
          name: X-NLX-Logrecord-ID
          schema:
            type: string
          description: Identifier of the request, traceable throughout the network
        - in: header
          name: X-Audit-Toelichting
          schema:
            type: string
          description: Toelichting waarom een bepaald verzoek wordt gedaan
        - in: path
          name: zaak_uuid
          schema:
            type: string
            format: uuid
          description: Unieke resource identifier (UUID4)
          required: true
      tags:
        - zaken
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ZaakBesluit"
        required: true
      security:
        - JWT-Claims:
            - zaken.bijwerken
      responses:
        "201":
          headers:
            Location:
              $ref: '#/components/headers/LocationHeader'
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ZaakBesluit"
          description: Created
        "400":
          $ref: '#/components/responses/ValidatieFoutResponse'
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
  /zaken/{zaak_uuid}/besluiten/{uuid}:
    get:
      operationId: zaakbesluit_retrieve
      description: Een specifiek ZAAKBESLUIT opvragen.
      summary: "Een specifiek ZAAKBESLUIT opvragen "
      parameters:
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
        - in: path
          name: zaak_uuid
          schema:
            type: string
            format: uuid
          description: Unieke resource identifier (UUID4)
          required: true
      tags:
        - zaken
      security:
        - JWT-Claims:
            - zaken.lezen
      responses:
        "200":
          headers:
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ZaakBesluit"
          description: OK
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
    delete:
      operationId: zaakbesluit_destroy
      description: |
        ***LET OP: Dit endpoint hoor je als consumer niet zelf aan te spreken.***

        De Besluiten API gebruikt dit endpoint om relaties te synchroniseren, daarom is dit endpoint in de Zaken API geimplementeerd.
      summary: Verwijder een ZAAKBESLUIT.
      parameters:
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
        - in: header
          name: X-NLX-Logrecord-ID
          schema:
            type: string
          description: Identifier of the request, traceable throughout the network
        - in: header
          name: X-Audit-Toelichting
          schema:
            type: string
          description: Toelichting waarom een bepaald verzoek wordt gedaan
        - in: path
          name: zaak_uuid
          schema:
            type: string
            format: uuid
          description: Unieke resource identifier (UUID4)
          required: true
      tags:
        - zaken
      security:
        - JWT-Claims:
            - zaken.bijwerken
      responses:
        "204":
          description: No content
          headers:
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
  /zaken/{zaak_uuid}/zaakeigenschappen:
    get:
      operationId: zaakeigenschap_list
      description: Alle ZAAKEIGENSCHAPpen opvragen.
      summary: "Alle ZAAKEIGENSCHAPpen opvragen. "
      parameters:
        - in: path
          name: zaak_uuid
          schema:
            type: string
            format: uuid
          description: Unieke resource identifier (UUID4)
          required: true
      tags:
        - zaken
      security:
        - JWT-Claims:
            - zaken.lezen
      responses:
        "200":
          headers:
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ZaakEigenschap"
          description: OK
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
    post:
      operationId: zaakeigenschap_create
      description: |
        Maak een ZAAKEIGENSCHAP aan.

        **Er wordt gevalideerd op:**
        - geldigheid `eigenschap` URL - de resource moet opgevraagd kunnen worden uit de Catalogi API en de vorm van een EIGENSCHAP hebben.
        - de `eigenschap` moet bij het `ZAAK.zaaktype` horen
      summary: Maak een ZAAKEIGENSCHAP aan.
      parameters:
        - in: header
          name: Content-Type
          schema:
            type: string
            enum:
              - application/json
          description: Content type van de verzoekinhoud.
          required: true
        - in: header
          name: X-NLX-Logrecord-ID
          schema:
            type: string
          description: Identifier of the request, traceable throughout the network
        - in: header
          name: X-Audit-Toelichting
          schema:
            type: string
          description: Toelichting waarom een bepaald verzoek wordt gedaan
        - in: path
          name: zaak_uuid
          schema:
            type: string
            format: uuid
          description: Unieke resource identifier (UUID4)
          required: true
      tags:
        - zaken
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ZaakEigenschap"
        required: true
      security:
        - JWT-Claims:
            - (zaken.bijwerken | zaken.geforceerd-bijwerken)
      responses:
        "201":
          headers:
            Location:
              $ref: '#/components/headers/LocationHeader'
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ZaakEigenschap"
          description: Created
        "400":
          $ref: '#/components/responses/ValidatieFoutResponse'
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
  /zaken/{zaak_uuid}/zaakeigenschappen/{uuid}:
    get:
      operationId: zaakeigenschap_retrieve
      description: Een specifieke ZAAKEIGENSCHAP opvragen.
      summary: Een specifieke ZAAKEIGENSCHAP opvragen.
      parameters:
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
        - in: header
          name: If-None-Match
          schema:
            type: string
          description: |
            Voer een voorwaardelijk verzoek uit. Deze header moet één of meerdere ETag-waardes bevatten van resources die de consumer gecached heeft. Indien de waarde van de ETag van de huidige resource voorkomt in deze set, dan antwoordt de provider met een lege HTTP 304 request. Zie [MDN](https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/If-None-Match) voor meer informatie.
          examples:
            OneValue:
              value: '"79054025255fb1a26e4bc422aef54eb4"'
              summary: "Eén ETag-waarde"
            MultipleValues:
              value: '"79054025255fb1a26e4bc422aef54eb4", "e4d909c290d0fb1ca068ffaddf22cbd0"'
              summary: Meerdere ETag-waardes
        - in: path
          name: zaak_uuid
          schema:
            type: string
            format: uuid
          description: Unieke resource identifier (UUID4)
          required: true
      tags:
        - zaken
      security:
        - JWT-Claims:
            - zaken.lezen
      responses:
        "200":
          headers:
            ETag:
              $ref: '#/components/headers/EtagHeader'
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ZaakEigenschap"
          description: OK
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
    put:
      operationId: zaakeigenschap_update
      description: |
        **Er wordt gevalideerd op** 
        - Alleen de `waarde` mag gewijzigd worden
      summary: Werk een ZAAKEIGENSCHAP in zijn geheel bij.
      parameters:
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
        - in: header
          name: Content-Type
          schema:
            type: string
            enum:
              - application/json
          description: Content type van de verzoekinhoud.
          required: true
        - in: path
          name: zaak_uuid
          schema:
            type: string
            format: uuid
          description: Unieke resource identifier (UUID4)
          required: true
      tags:
        - zaken
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ZaakEigenschap"
        required: true
      security:
        - JWT-Claims:
            - (zaken.bijwerken | zaken.geforceerd-bijwerken)
      responses:
        "200":
          headers:
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ZaakEigenschap"
          description: OK
        "400":
          $ref: '#/components/responses/ValidatieFoutResponse'
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
    patch:
      operationId: zaakeigenschap_partial_update
      description: |
        **Er wordt gevalideerd op** 
        - Alleen de `waarde` mag gewijzigd worden
      summary: Werk een ZAAKEIGENSCHAP deels bij.
      parameters:
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
        - in: header
          name: Content-Type
          schema:
            type: string
            enum:
              - application/json
          description: Content type van de verzoekinhoud.
          required: true
        - in: path
          name: zaak_uuid
          schema:
            type: string
            format: uuid
          description: Unieke resource identifier (UUID4)
          required: true
      tags:
        - zaken
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PatchedZaakEigenschap"
      security:
        - JWT-Claims:
            - (zaken.bijwerken | zaken.geforceerd-bijwerken)
      responses:
        "200":
          headers:
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ZaakEigenschap"
          description: OK
        "400":
          $ref: '#/components/responses/ValidatieFoutResponse'
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
    delete:
      operationId: zaakeigenschap_destroy
      description: Verwijder een ZAAKEIGENSCHAP
      summary: Verwijder een ZAAKEIGENSCHAP.
      parameters:
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
        - in: path
          name: zaak_uuid
          schema:
            type: string
            format: uuid
          description: Unieke resource identifier (UUID4)
          required: true
      tags:
        - zaken
      security:
        - JWT-Claims:
            - (zaken.bijwerken | zaken.geforceerd-bijwerken)
      responses:
        "204":
          description: No content
          headers:
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
    head:
      operationId: zaakeigenschap_headers
      description: Vraag de headers op die je bij een GET request zou krijgen.
      summary: "De headers voor een specifiek(e) ZAAKEIGENSCHAP opvragen "
      parameters:
        - in: path
          name: uuid
          schema:
            type: string
            format: uuid
            description: Unieke resource identifier (UUID4)
          required: true
        - in: header
          name: If-None-Match
          schema:
            type: string
          description: |
            Voer een voorwaardelijk verzoek uit. Deze header moet één of meerdere ETag-waardes bevatten van resources die de consumer gecached heeft. Indien de waarde van de ETag van de huidige resource voorkomt in deze set, dan antwoordt de provider met een lege HTTP 304 request. Zie [MDN](https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/If-None-Match) voor meer informatie.
          examples:
            OneValue:
              value: '"79054025255fb1a26e4bc422aef54eb4"'
              summary: "Eén ETag-waarde"
            MultipleValues:
              value: '"79054025255fb1a26e4bc422aef54eb4", "e4d909c290d0fb1ca068ffaddf22cbd0"'
              summary: Meerdere ETag-waardes
        - in: path
          name: zaak_uuid
          schema:
            type: string
            format: uuid
          description: Unieke resource identifier (UUID4)
          required: true
      tags:
        - zaken
      responses:
        "200":
          headers:
            ETag:
              $ref: '#/components/headers/EtagHeader'
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          description: OK
  /zaken/_zoek:
    post:
      operationId: zaak__zoek
      description: |
        Zoeken/filteren gaat normaal via de `list` operatie, deze is echter niet geschikt voor geo-zoekopdrachten.
      summary: Voer een (geo)-zoekopdracht uit op ZAAKen.
      parameters:
        - in: header
          name: Content-Type
          schema:
            type: string
            enum:
              - application/json
          description: Content type van de verzoekinhoud.
          required: true
        - in: query
          name: page
          schema:
            type: integer
          description: Een pagina binnen de gepagineerde set resultaten.
        - in: header
          name: Accept-Crs
          schema:
            type: string
            enum:
              - EPSG:4326
          description: |
            Het gewenste 'Coordinate Reference System' (CRS) van de geometrie in het antwoord (response body). Volgens de GeoJSON spec is WGS84 de default (EPSG:4326 is hetzelfde als WGS84).
          required: true
        - in: header
          name: Content-Crs
          schema:
            type: string
            enum:
              - EPSG:4326
          description: |
            Het 'Coordinate Reference System' (CRS) van de geometrie in de vraag (request body). Volgens de GeoJSON spec is WGS84 de default (EPSG:4326 is hetzelfde als WGS84).
          required: true
      tags:
        - zaken
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ZaakZoek"
      security:
        - JWT-Claims:
            - zaken.lezen
      responses:
        "200":
          headers:
            Content-Crs:
              $ref: '#/components/headers/ContentCrsHeader'
            API-Version:
              $ref: '#/components/headers/ApiVersionHeader'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginatedZaakList"
          description: OK
        "400":
          $ref: '#/components/responses/ValidatieFoutResponse'
        '4XX':
          $ref: '#/components/responses/FoutResponse'
        "500":
          $ref: '#/components/responses/FoutResponse'
components:
  responses:
    FoutResponse:
      headers:
        API-Version:
          $ref: '#/components/headers/ApiVersionHeader'
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/Fout"
      description: Fout - fout
    ValidatieFoutResponse:
      headers:
        API-Version:
          $ref: '#/components/headers/ApiVersionHeader'
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ValidatieFout"
      description: ValidatieFout - validatiefout
  schemas:
    AardRelatieEnum:
      description: |-
        Benamingen van de aard van de relaties van andere zaken tot (onderhanden) zaken.
      type: string
      enum:
        - vervolg
        - onderwerp
        - bijdrage
      x-enumDescriptions:
        vervolg: De andere zaak gaf aanleiding tot het starten van de onderhanden
          zaak.
        onderwerp: De andere zaak is relevant voor cq. is onderwerp van de onderhanden
          zaak.
        bijdrage: Aan het bereiken van de uitkomst van de andere zaak levert de onderhanden
          zaak een bijdrage.
    AardRelatieWeergaveEnum:
      type: string
      enum:
        - "Hoort bij, omgekeerd: kent"
        - "Legt vast, omgekeerd: kan vastgelegd zijn als"
    ArchiefnominatieEnum:
      type: string
      enum:
        - blijvend_bewaren
        - vernietigen
    ArchiefstatusEnum:
      description: |-
        Aanduiding of het zaakdossier blijvend bewaard of na een bepaalde termijn vernietigd moet worden.
      type: string
      enum:
        - nog_te_archiveren
        - gearchiveerd
        - gearchiveerd_procestermijn_onbekend
        - overgedragen
      x-enumDescriptions:
        nog_te_archiveren: De zaak cq. het zaakdossier is nog niet als geheel gearchiveerd.
        gearchiveerd: De zaak cq. het zaakdossier is als geheel niet-wijzigbaar bewaarbaar
          gemaakt.
        gearchiveerd_procestermijn_onbekend: De zaak cq. het zaakdossier is als geheel
          niet-wijzigbaar bewaarbaar gemaakt maar de vernietigingsdatum kan nog niet
          bepaald worden.
        overgedragen: De zaak cq. het zaakdossier is overgebracht naar een archiefbewaarplaats.
    AuditTrail:
      type: object
      properties:
        uuid:
          description: Unieke identificatie van de audit regel.
          type: string
          format: uuid
        bron:
          $ref: "#/components/schemas/BronEnum"
        applicatieId:
          description: Unieke identificatie van de applicatie, binnen de organisatie.
          type: string
          maxLength: 100
        applicatieWeergave:
          description: Vriendelijke naam van de applicatie.
          type: string
          maxLength: 200
        gebruikersId:
          description: |
            Unieke identificatie van de gebruiker die binnen de organisatie herleid kan worden naar een persoon.
          type: string
          maxLength: 255
        gebruikersWeergave:
          description: Vriendelijke naam van de gebruiker.
          type: string
          maxLength: 255
        actie:
          description: |
            De uitgevoerde handeling.

            De bekende waardes voor dit veld zijn hieronder aangegeven,                         maar andere waardes zijn ook toegestaan

            Uitleg bij mogelijke waarden:

            * `create` - Object aangemaakt
            * `list` - Lijst van objecten opgehaald
            * `retrieve` - Object opgehaald
            * `destroy` - Object verwijderd
            * `update` - Object bijgewerkt
            * `partial_update` - Object deels bijgewerkt
          type: string
          maxLength: 50
        actieWeergave:
          description: Vriendelijke naam van de actie.
          type: string
          maxLength: 200
        resultaat:
          description: HTTP status code van de API response van de uitgevoerde handeling.
          type: integer
          maximum: 599
          minimum: 100
        hoofdObject:
          description: De URL naar het hoofdobject van een component.
          type: string
          format: uri
          maxLength: 1000
        resource:
          description: Het type resource waarop de actie gebeurde.
          type: string
          maxLength: 50
        resourceUrl:
          description: De URL naar het object.
          type: string
          format: uri
          maxLength: 1000
        toelichting:
          description: Toelichting waarom de handeling is uitgevoerd.
          type: string
        resourceWeergave:
          description: Vriendelijke identificatie van het object.
          type: string
          maxLength: 200
        aanmaakdatum:
          description: De datum waarop de handeling is gedaan.
          type: string
          format: date-time
          readOnly: true
        wijzigingen:
          $ref: "#/components/schemas/Wijzigingen"
      required:
        - aanmaakdatum
        - actie
        - bron
        - hoofdObject
        - resource
        - resourceUrl
        - resourceWeergave
        - resultaat
        - wijzigingen
    BetalingsindicatieEnum:
      type: string
      enum:
        - nvt
        - nog_niet
        - gedeeltelijk
        - geheel
    BetrokkeneTypeEnum:
      description: |-
        Type van de `betrokkene`.
      type: string
      enum:
        - natuurlijk_persoon
        - niet_natuurlijk_persoon
        - vestiging
        - organisatorische_eenheid
        - medewerker
      x-enumDescriptions:
        natuurlijk_persoon: Natuurlijk persoon
        niet_natuurlijk_persoon: Niet-natuurlijk persoon
        vestiging: Vestiging
        organisatorische_eenheid: Organisatorische eenheid
        medewerker: Medewerker
    BlankEnum:
      enum:
        - ""
    BronEnum:
      description: |-
        De naam van het component waar de wijziging in is gedaan.
      type: string
      enum:
        - ac
        - nrc
        - zrc
        - ztc
        - drc
        - brc
        - cmc
        - kc
        - vrc
      x-enumDescriptions:
        ac: Autorisaties API
        nrc: Notificaties API
        zrc: Zaken API
        ztc: Catalogi API
        drc: Documenten API
        brc: Besluiten API
        cmc: Contactmomenten API
        kc: Klanten API
        vrc: Verzoeken API
    ContactPersoonRol:
      description: |
        De gegevens van de persoon die anderen desgevraagd in contact brengt met medewerkers van de BETROKKENE, een NIET-NATUURLIJK PERSOON of VESTIGING zijnde, of met BETROKKENE zelf, een NATUURLIJK PERSOON zijnde , vanuit het belang van BETROKKENE in haar ROL bij een ZAAK.
      type: object
      properties:
        emailadres:
          title: Email
          description: Elektronich postadres waaronder de contactpersoon in de regel
            bereikbaar is.
          type: string
          format: email
          maxLength: 254
        functie:
          description: |
            De aanduiding van de taken, rechten en plichten die de contactpersoon heeft binnen de organisatie van BETROKKENE.
          type: string
          maxLength: 50
        telefoonnummer:
          description: Telefoonnummer waaronder de contactpersoon in de regel bereikbaar
            is.
          type: string
          maxLength: 20
        naam:
          description: De opgemaakte naam van de contactpersoon namens de BETROKKENE.
          type: string
          maxLength: 40
      required:
        - naam
      nullable: true
    FieldValidationError:
      description: Formaat van validatiefouten.
      type: object
      properties:
        name:
          description: Naam van het veld met ongeldige gegevens
          type: string
        code:
          description: Systeemcode die het type fout aangeeft
          type: string
        reason:
          description: Uitleg wat er precies fout is met de gegevens
          type: string
      required:
        - code
        - name
        - reason
    Fout:
      description: Formaat van HTTP 4xx en 5xx fouten.
      type: object
      properties:
        title:
          description: Generieke titel voor het type fout
          type: string
        type:
          description: URI referentie naar het type fout, bedoeld voor developers
          type: string
        code:
          description: Systeemcode die het type fout aangeeft
          type: string
        status:
          description: De HTTP status code
          type: integer
        detail:
          description: Extra informatie bij de fout, indien beschikbaar
          type: string
        instance:
          description: |
            URI met referentie naar dit specifiek voorkomen van de fout. Deze kan gebruikt worden in combinatie met server logs, bijvoorbeeld.
          type: string
      required:
        - code
        - detail
        - instance
        - status
        - title
    GeoJSONGeometry:
      oneOf:
        - $ref: "#/components/schemas/Point"
        - $ref: "#/components/schemas/MultiPoint"
        - $ref: "#/components/schemas/LineString"
        - $ref: "#/components/schemas/MultiLineString"
        - $ref: "#/components/schemas/Polygon"
        - $ref: "#/components/schemas/MultiPolygon"
        - $ref: "#/components/schemas/GeometryCollection"
      description: Punt, lijn of (multi-)vlak geometrie-informatie, in GeoJSON.
      type: object
      discriminator:
        propertyName: type
    GeoWithin:
      type: object
      properties:
        within:
          $ref: "#/components/schemas/GeoJSONGeometry"
    Geometry:
      description: GeoJSON geometry
      type: object
      properties:
        type:
          allOf:
            - $ref: "#/components/schemas/GeometryTypeEnum"
          description: The geometry type
      required:
        - type
      externalDocs:
        url: https://tools.ietf.org/html/rfc7946#section-3.1
    GeometryCollection:
      allOf:
        - $ref: "#/components/schemas/Geometry"
        - type: object
          required:
            - geometries
          properties:
            geometries:
              type: array
              items:
                $ref: "#/components/schemas/Geometry"
      description: GeoJSON multi-polygon geometry
      type: object
      externalDocs:
        url: https://tools.ietf.org/html/rfc7946#section-3.1.8
    GeometryTypeEnum:
      type: string
      enum:
        - Point
        - MultiPoint
        - LineString
        - MultiLineString
        - Polygon
        - MultiPolygon
        - Feature
        - FeatureCollection
        - GeometryCollection
    GeslachtsaanduidingEnum:
      type: string
      enum:
        - m
        - v
        - o
    IndicatieMachtigingEnum:
      type: string
      enum:
        - gemachtigde
        - machtiginggever
    InnRechtsvormEnum:
      type: string
      enum:
        - besloten_vennootschap
        - cooperatie_europees_economische_samenwerking
        - europese_cooperatieve_venootschap
        - europese_naamloze_vennootschap
        - kerkelijke_organisatie
        - naamloze_vennootschap
        - onderlinge_waarborg_maatschappij
        - overig_privaatrechtelijke_rechtspersoon
        - stichting
        - vereniging
        - vereniging_van_eigenaars
        - publiekrechtelijke_rechtspersoon
        - vennootschap_onder_firma
        - maatschap
        - rederij
        - commanditaire_vennootschap
        - kapitaalvennootschap_binnen_eer
        - overige_buitenlandse_rechtspersoon_vennootschap
        - kapitaalvennootschap_buiten_eer
    KlantContact:
      type: object
      properties:
        url:
          description: |
            URL-referentie naar dit object. Dit is de unieke identificatie en locatie van dit object.
          type: string
          format: uri
          readOnly: true
          minLength: 1
          maxLength: 1000
        uuid:
          description: Unieke resource identifier (UUID4)
          type: string
          format: uuid
          readOnly: true
        zaak:
          description: URL-referentie naar de ZAAK.
          type: string
          format: uri
          minLength: 1
          maxLength: 1000
        identificatie:
          description: De unieke aanduiding van een KLANTCONTACT
          type: string
          maxLength: 14
        datumtijd:
          description: De datum en het tijdstip waarop het KLANTCONTACT begint
          type: string
          format: date-time
        kanaal:
          description: Het communicatiekanaal waarlangs het KLANTCONTACT gevoerd wordt
          type: string
          maxLength: 20
        onderwerp:
          description: Het onderwerp waarover contact is geweest met de klant.
          type: string
          maxLength: 200
        toelichting:
          description: Een toelichting die inhoudelijk het contact met de klant beschrijft.
          type: string
          maxLength: 1000
      required:
        - datumtijd
        - url
        - uuid
        - zaak
    LineString:
      allOf:
        - $ref: "#/components/schemas/Geometry"
        - type: object
          required:
            - coordinates
          properties:
            coordinates:
              type: array
              items:
                $ref: "#/components/schemas/Point2D"
              minItems: 2
      title: Line-string
      description: GeoJSON line-string geometry
      type: object
      externalDocs:
        url: https://tools.ietf.org/html/rfc7946#section-3.1.4
    MultiLineString:
      allOf:
        - $ref: "#/components/schemas/Geometry"
        - type: object
          required:
            - coordinates
          properties:
            coordinates:
              type: array
              items:
                type: array
                items:
                  $ref: "#/components/schemas/Point2D"
      title: Multi-line string
      description: GeoJSON multi-line-string geometry
      type: object
      externalDocs:
        url: https://tools.ietf.org/html/rfc7946#section-3.1.5
    MultiPoint:
      allOf:
        - $ref: "#/components/schemas/Geometry"
        - type: object
          required:
            - coordinates
          properties:
            coordinates:
              type: array
              items:
                $ref: "#/components/schemas/Point2D"
      description: GeoJSON multi-point geometry
      type: object
      externalDocs:
        url: https://tools.ietf.org/html/rfc7946#section-3.1.3
    MultiPolygon:
      allOf:
        - $ref: "#/components/schemas/Geometry"
        - type: object
          required:
            - coordinates
          properties:
            coordinates:
              type: array
              items:
                type: array
                items:
                  type: array
                  items:
                    $ref: "#/components/schemas/Point2D"
      title: Multi-polygon
      description: GeoJSON multi-polygon geometry
      type: object
      externalDocs:
        url: https://tools.ietf.org/html/rfc7946#section-3.1.7
    NullEnum:
      enum:
        - 
    ObjectTypeEnum:
      description: |-
        Beschrijft het type OBJECT gerelateerd aan de ZAAK. Als er geen passend type is, dan moet het type worden opgegeven onder `objectTypeOverige`.
      type: string
      enum:
        - adres
        - besluit
        - buurt
        - enkelvoudig_document
        - gemeente
        - gemeentelijke_openbare_ruimte
        - huishouden
        - inrichtingselement
        - kadastrale_onroerende_zaak
        - kunstwerkdeel
        - maatschappelijke_activiteit
        - medewerker
        - natuurlijk_persoon
        - niet_natuurlijk_persoon
        - openbare_ruimte
        - organisatorische_eenheid
        - pand
        - spoorbaandeel
        - status
        - terreindeel
        - terrein_gebouwd_object
        - vestiging
        - waterdeel
        - wegdeel
        - wijk
        - woonplaats
        - woz_deelobject
        - woz_object
        - woz_waarde
        - zakelijk_recht
        - overige
      x-enumDescriptions:
        adres: Adres
        besluit: Besluit
        buurt: Buurt
        enkelvoudig_document: Enkelvoudig document
        gemeente: Gemeente
        gemeentelijke_openbare_ruimte: Gemeentelijke openbare ruimte
        huishouden: Huishouden
        inrichtingselement: Inrichtingselement
        kadastrale_onroerende_zaak: Kadastrale onroerende zaak
        kunstwerkdeel: Kunstwerkdeel
        maatschappelijke_activiteit: Maatschappelijke activiteit
        medewerker: Medewerker
        natuurlijk_persoon: Natuurlijk persoon
        niet_natuurlijk_persoon: Niet-natuurlijk persoon
        openbare_ruimte: Openbare ruimte
        organisatorische_eenheid: Organisatorische eenheid
        pand: Pand
        spoorbaandeel: Spoorbaandeel
        status: Status
        terreindeel: Terreindeel
        terrein_gebouwd_object: Terrein gebouwd object
        vestiging: Vestiging
        waterdeel: Waterdeel
        wegdeel: Wegdeel
        wijk: Wijk
        woonplaats: Woonplaats
        woz_deelobject: Woz deel object
        woz_object: Woz object
        woz_waarde: Woz waarde
        zakelijk_recht: Zakelijk recht
        overige: Overige
    Opschorting:
      description: Gegevens omtrent het tijdelijk opschorten van de behandeling van
        de ZAAK
      type: object
      properties:
        indicatie:
          title: indicatie opschorting
          description: Aanduiding of de behandeling van de ZAAK tijdelijk is opgeschort.
          type: boolean
        reden:
          title: reden opschorting
          description: Omschrijving van de reden voor het opschorten van de behandeling
            van de zaak.
          type: string
          maxLength: 200
      required:
        - indicatie
        - reden
      nullable: true
    PaginatedKlantContactList:
      type: object
      properties:
        count:
          type: integer
          example: 123
        next:
          type: string
          format: uri
          example: http://api.example.org/accounts/?page=4
          nullable: true
        previous:
          type: string
          format: uri
          example: http://api.example.org/accounts/?page=2
          nullable: true
        results:
          type: array
          items:
            $ref: "#/components/schemas/KlantContact"
    PaginatedResultaatList:
      type: object
      properties:
        count:
          type: integer
          example: 123
        next:
          type: string
          format: uri
          example: http://api.example.org/accounts/?page=4
          nullable: true
        previous:
          type: string
          format: uri
          example: http://api.example.org/accounts/?page=2
          nullable: true
        results:
          type: array
          items:
            $ref: "#/components/schemas/Resultaat"
    PaginatedRolList:
      type: object
      properties:
        count:
          type: integer
          example: 123
        next:
          type: string
          format: uri
          example: http://api.example.org/accounts/?page=4
          nullable: true
        previous:
          type: string
          format: uri
          example: http://api.example.org/accounts/?page=2
          nullable: true
        results:
          type: array
          items:
            $ref: "#/components/schemas/RolVariant"
    PaginatedStatusList:
      type: object
      properties:
        count:
          type: integer
          example: 123
        next:
          type: string
          format: uri
          example: http://api.example.org/accounts/?page=4
          nullable: true
        previous:
          type: string
          format: uri
          example: http://api.example.org/accounts/?page=2
          nullable: true
        results:
          type: array
          items:
            $ref: "#/components/schemas/Status"
    PaginatedZaakList:
      type: object
      properties:
        count:
          type: integer
          example: 123
        next:
          type: string
          format: uri
          example: http://api.example.org/accounts/?page=4
          nullable: true
        previous:
          type: string
          format: uri
          example: http://api.example.org/accounts/?page=2
          nullable: true
        results:
          type: array
          items:
            $ref: "#/components/schemas/ZaakExpanded"
    PaginatedZaakObjectList:
      type: object
      properties:
        count:
          type: integer
          example: 123
        next:
          type: string
          format: uri
          example: http://api.example.org/accounts/?page=4
          nullable: true
        previous:
          type: string
          format: uri
          example: http://api.example.org/accounts/?page=2
          nullable: true
        results:
          type: array
          items:
            $ref: "#/components/schemas/ZaakObjectVariant"
    PatchedResultaat:
      type: object
      properties:
        url:
          description: |
            URL-referentie naar dit object. Dit is de unieke identificatie en locatie van dit object.
          type: string
          format: uri
          readOnly: true
          minLength: 1
          maxLength: 1000
        uuid:
          description: Unieke resource identifier (UUID4)
          type: string
          format: uuid
          readOnly: true
        zaak:
          description: URL-referentie naar de ZAAK.
          type: string
          format: uri
          minLength: 1
          maxLength: 1000
        resultaattype:
          description: URL-referentie naar het RESULTAATTYPE (in de Catalogi API).
          type: string
          format: uri
          maxLength: 1000
        toelichting:
          description: Een toelichting op wat het resultaat van de zaak inhoudt.
          type: string
          maxLength: 1000
    PatchedZaak:
      description: |
        Set gegevensgroepdata from validated nested data.

        Usage: include the mixin on the ModelSerializer that has gegevensgroepen.
      type: object
      properties:
        url:
          description: |
            URL-referentie naar dit object. Dit is de unieke identificatie en locatie van dit object.
          type: string
          format: uri
          readOnly: true
          minLength: 1
          maxLength: 1000
        uuid:
          description: Unieke resource identifier (UUID4)
          type: string
          format: uuid
          readOnly: true
        identificatie:
          description: |
            De unieke identificatie van de ZAAK binnen de organisatie die verantwoordelijk is voor de behandeling van de ZAAK.
          type: string
          maxLength: 40
        bronorganisatie:
          description: |
            Het RSIN van de Niet-natuurlijk persoon zijnde de organisatie die de zaak heeft gecreeerd. Dit moet een geldig RSIN zijn van 9 nummers en voldoen aan https://nl.wikipedia.org/wiki/Burgerservicenummer#11-proef
          type: string
          maxLength: 9
        omschrijving:
          description: Een korte omschrijving van de zaak.
          type: string
          maxLength: 80
        toelichting:
          description: Een toelichting op de zaak.
          type: string
          maxLength: 1000
        zaaktype:
          description: |
            URL-referentie naar het ZAAKTYPE (in de Catalogi API) in de CATALOGUS waar deze voorkomt
          type: string
          format: uri
          maxLength: 1000
        registratiedatum:
          description: |
            De datum waarop de zaakbehandelende organisatie de ZAAK heeft geregistreerd. Indien deze niet opgegeven wordt, wordt de datum van vandaag gebruikt.
          type: string
          format: date
        verantwoordelijkeOrganisatie:
          description: |
            Het RSIN van de Niet-natuurlijk persoon zijnde de organisatie die eindverantwoordelijk is voor de behandeling van de zaak. Dit moet een geldig RSIN zijn van 9 nummers en voldoen aan https://nl.wikipedia.org/wiki/Burgerservicenummer#11-proef
          type: string
          maxLength: 9
        startdatum:
          description: De datum waarop met de uitvoering van de zaak is gestart
          type: string
          format: date
        einddatum:
          description: De datum waarop de uitvoering van de zaak afgerond is.
          type: string
          format: date
          readOnly: true
          nullable: true
        einddatumGepland:
          description: De datum waarop volgens de planning verwacht wordt dat de zaak
            afgerond wordt.
          type: string
          format: date
          nullable: true
        uiterlijkeEinddatumAfdoening:
          description: |
            De laatste datum waarop volgens wet- en regelgeving de zaak afgerond dient te zijn.
          type: string
          format: date
          nullable: true
        publicatiedatum:
          description: Datum waarop (het starten van) de zaak gepubliceerd is of wordt.
          type: string
          format: date
          nullable: true
        communicatiekanaal:
          description: |
            Het medium waarlangs de aanleiding om een zaak te starten is ontvangen. URL naar een communicatiekanaal in de VNG-Referentielijst van communicatiekanalen.
          type: string
          format: uri
          maxLength: 1000
        productenOfDiensten:
          description: |
            De producten en/of diensten die door de zaak worden voortgebracht. Dit zijn URLs naar de resources zoals die door de producten- en dienstencatalogus-API wordt ontsloten. De producten/diensten moeten bij het zaaktype vermeld zijn.
          type: array
          items:
            title: URL naar product/dienst
            type: string
            format: uri
            maxLength: 1000
        vertrouwelijkheidaanduiding:
          $ref: "#/components/schemas/VertrouwelijkheidaanduidingEnum"
        betalingsindicatie:
          oneOf:
            - $ref: "#/components/schemas/BetalingsindicatieEnum"
            - $ref: "#/components/schemas/BlankEnum"
          description: |
            Indicatie of de, met behandeling van de zaak gemoeide, kosten betaald zijn door de desbetreffende betrokkene.

            Uitleg bij mogelijke waarden:

            * `nvt` - Er is geen sprake van te betalen, met de zaak gemoeide, kosten.
            * `nog_niet` - De met de zaak gemoeide kosten zijn (nog) niet betaald.
            * `gedeeltelijk` - De met de zaak gemoeide kosten zijn gedeeltelijk betaald.
            * `geheel` - De met de zaak gemoeide kosten zijn geheel betaald.
        betalingsindicatieWeergave:
          description: Uitleg bij `betalingsindicatie`.
          type: string
          readOnly: true
        laatsteBetaaldatum:
          description: |
            De datum waarop de meest recente betaling is verwerkt van kosten die gemoeid zijn met behandeling van de zaak.
          type: string
          format: date-time
          nullable: true
        zaakgeometrie:
          allOf:
            - $ref: "#/components/schemas/GeoJSONGeometry"
          nullable: true
        verlenging:
          allOf:
            - $ref: "#/components/schemas/Verlenging"
          nullable: true
        opschorting:
          allOf:
            - $ref: "#/components/schemas/Opschorting"
          nullable: true
        selectielijstklasse:
          description: |
            URL-referentie naar de categorie in de gehanteerde 'Selectielijst Archiefbescheiden' die, gezien het zaaktype en het resultaattype van de zaak, bepalend is voor het archiefregime van de zaak.
          type: string
          format: uri
          maxLength: 1000
        hoofdzaak:
          title: Is deelzaak van
          description: |
            URL-referentie naar de ZAAK, waarom verzocht is door de initiator daarvan, die behandeld wordt in twee of meer separate ZAAKen waarvan de onderhavige ZAAK er één is.
          type: string
          format: uri
          nullable: true
          minLength: 1
          maxLength: 1000
        deelzaken:
          title: is deelzaak van
          description: URL-referenties naar deel ZAAKen.
          type: array
          items:
            title: ""
            type: string
            format: uri
          readOnly: true
          uniqueItems: true
        relevanteAndereZaken:
          title: zaak
          description: Een lijst van relevante andere zaken.
          type: array
          items:
            $ref: "#/components/schemas/RelevanteZaak"
        eigenschappen:
          title: zaak
          type: array
          items:
            title: ""
            type: string
            format: uri
          readOnly: true
          uniqueItems: true
        rollen:
          title: zaak
          type: array
          items:
            title: ""
            type: string
            format: uri
          readOnly: true
          uniqueItems: true
        status:
          description: Indien geen status bekend is, dan is de waarde 'null'
          type: string
          format: uri
          readOnly: true
          nullable: true
        zaakinformatieobjecten:
          title: zaak
          type: array
          items:
            title: ""
            type: string
            format: uri
          readOnly: true
          uniqueItems: true
        zaakobjecten:
          title: zaak
          type: array
          items:
            title: ""
            type: string
            format: uri
          readOnly: true
          uniqueItems: true
        kenmerken:
          title: zaak
          description: |
            Lijst van kenmerken. Merk op dat refereren naar gerelateerde objecten beter kan via `ZaakObject`.
          type: array
          items:
            $ref: "#/components/schemas/ZaakKenmerk"
        archiefnominatie:
          oneOf:
            - $ref: "#/components/schemas/ArchiefnominatieEnum"
            - $ref: "#/components/schemas/BlankEnum"
            - $ref: "#/components/schemas/NullEnum"
          description: |
            Aanduiding of het zaakdossier blijvend bewaard of na een bepaalde termijn vernietigd moet worden.

            Uitleg bij mogelijke waarden:

            * `blijvend_bewaren` - Het zaakdossier moet bewaard blijven en op de Archiefactiedatum overgedragen worden naar een archiefbewaarplaats.
            * `vernietigen` - Het zaakdossier moet op of na de Archiefactiedatum vernietigd worden.
          nullable: true
        archiefstatus:
          $ref: "#/components/schemas/ArchiefstatusEnum"
        archiefactiedatum:
          description: |
            De datum waarop het gearchiveerde zaakdossier vernietigd moet worden dan wel overgebracht moet worden naar een archiefbewaarplaats. Wordt automatisch berekend bij het aanmaken of wijzigen van een RESULTAAT aan deze ZAAK indien nog leeg.
          type: string
          format: date
          nullable: true
        resultaat:
          description: |
            URL-referentie naar het RESULTAAT. Indien geen resultaat bekend is, dan is de waarde 'null'
          type: string
          format: uri
          readOnly: true
          nullable: true
        opdrachtgevendeOrganisatie:
          description: |
            De krachtens publiekrecht ingestelde rechtspersoon dan wel ander niet-natuurlijk persoon waarbinnen het (bestuurs)orgaan zetelt dat opdracht heeft gegeven om taken uit te voeren waaraan de zaak invulling geeft.
          type: string
          maxLength: 9
        processobjectaard:
          title: Procesobjectaard
          description: |
            Omschrijving van het object, subject of gebeurtenis waarop, vanuit archiveringsoptiek, de zaak betrekking heeft.
          type: string
          nullable: true
          maxLength: 200
        resultaattoelichting:
          description: Een toelichting op wat het resultaat van de zaak inhoudt.
          type: string
          maxLength: 1000
        startdatumBewaartermijn:
          description: |
            De datum die de start markeert van de termijn waarop het zaakdossier vernietigd moet worden.
          type: string
          format: date
          nullable: true
        processobject:
          allOf:
            - $ref: "#/components/schemas/Processobject"
          nullable: true
    PatchedZaakEigenschap:
      description: |
        A type of `ModelSerializer` that uses hyperlinked relationships with compound keys instead
        of primary key relationships.  Specifically:

        * A 'url' field is included instead of the 'id' field.
        * Relationships to other instances are hyperlinks, instead of primary keys.

        NOTE: this only works with DRF 3.1.0 and above.
      type: object
      properties:
        url:
          type: string
          format: uri
          readOnly: true
        uuid:
          description: Unieke resource identifier (UUID4)
          type: string
          format: uuid
          readOnly: true
        zaak:
          type: string
          format: uri
        eigenschap:
          description: URL-referentie naar de EIGENSCHAP (in de Catalogi API).
          type: string
          format: uri
          maxLength: 1000
        naam:
          description: De naam van de EIGENSCHAP (overgenomen uit de Catalogi API).
          type: string
          readOnly: true
        waarde:
          type: string
    PatchedZaakInformatieObject:
      type: object
      properties:
        url:
          description: |
            URL-referentie naar dit object. Dit is de unieke identificatie en locatie van dit object.
          type: string
          format: uri
          readOnly: true
          minLength: 1
          maxLength: 1000
        uuid:
          description: Unieke resource identifier (UUID4)
          type: string
          format: uuid
          readOnly: true
        informatieobject:
          description: |
            URL-referentie naar het INFORMATIEOBJECT (in de Documenten API), waar ook de relatieinformatie opgevraagd kan worden.
          type: string
          format: uri
          maxLength: 1000
        zaak:
          description: URL-referentie naar de ZAAK.
          type: string
          format: uri
          minLength: 1
          maxLength: 1000
        aardRelatieWeergave:
          allOf:
            - $ref: "#/components/schemas/AardRelatieWeergaveEnum"
          readOnly: true
        titel:
          description: De naam waaronder het INFORMATIEOBJECT binnen het OBJECT bekend
            is.
          type: string
          maxLength: 200
        beschrijving:
          description: Een op het object gerichte beschrijving van de inhoud vanhet
            INFORMATIEOBJECT.
          type: string
        registratiedatum:
          description: |
            De datum waarop de behandelende organisatie het INFORMATIEOBJECT heeft geregistreerd bij het OBJECT. Geldige waardes zijn datumtijden gelegen op of voor de huidige datum en tijd.
          type: string
          format: date-time
          readOnly: true
        vernietigingsdatum:
          description: De datum waarop het informatieobject uit het zaakdossier verwijderd
            moet worden.
          type: string
          format: date-time
          nullable: true
        status:
          description: |
            De bij de desbetreffende ZAAK behorende STATUS waarvoor het ZAAK-INFORMATIEOBJECT relevant is (geweest) met het oog op het bereiken van die STATUS en/of de communicatie daarover.
          type: string
          format: uri
          nullable: true
          minLength: 1
          maxLength: 1000
    Point:
      allOf:
        - $ref: "#/components/schemas/Geometry"
        - type: object
          required:
            - coordinates
          properties:
            coordinates:
              $ref: "#/components/schemas/Point2D"
      description: GeoJSON point geometry
      type: object
      externalDocs:
        url: https://tools.ietf.org/html/rfc7946#section-3.1.2
    Point2D:
      type: array
      items:
        type: number
      maxItems: 2
      minItems: 2
    Polygon:
      allOf:
        - $ref: "#/components/schemas/Geometry"
        - type: object
          required:
            - coordinates
          properties:
            coordinates:
              type: array
              items:
                type: array
                items:
                  $ref: "#/components/schemas/Point2D"
      description: GeoJSON polygon geometry
      type: object
      externalDocs:
        url: https://tools.ietf.org/html/rfc7946#section-3.1.6
    Processobject:
      description: |
        Specificatie van de attribuutsoort van het object, subject of gebeurtenis  waarop, vanuit archiveringsoptiek, de zaak betrekking heeft en dat bepalend is voor de start van de archiefactietermijn.
      type: object
      properties:
        datumkenmerk:
          description: |
            De naam van de attribuutsoort van het procesobject dat bepalend is voor het einde van de procestermijn.
          type: string
          maxLength: 250
        identificatie:
          description: De unieke aanduiding van het procesobject.
          type: string
          maxLength: 250
        objecttype:
          description: Het soort object dat het procesobject representeert.
          type: string
          maxLength: 250
        registratie:
          description: De naam van de registratie waarvan het procesobject deel uit
            maakt.
          type: string
          maxLength: 250
      required:
        - datumkenmerk
        - identificatie
        - objecttype
        - registratie
      nullable: true
    RelevanteZaak:
      type: object
      properties:
        url:
          title: URL-referentie naar de ZAAK.
          type: string
          format: uri
          maxLength: 1000
        aardRelatie:
          $ref: "#/components/schemas/AardRelatieEnum"
      required:
        - aardRelatie
        - url
    Resultaat:
      type: object
      properties:
        url:
          description: |
            URL-referentie naar dit object. Dit is de unieke identificatie en locatie van dit object.
          type: string
          format: uri
          readOnly: true
          minLength: 1
          maxLength: 1000
        uuid:
          description: Unieke resource identifier (UUID4)
          type: string
          format: uuid
          readOnly: true
        zaak:
          description: URL-referentie naar de ZAAK.
          type: string
          format: uri
          minLength: 1
          maxLength: 1000
        resultaattype:
          description: URL-referentie naar het RESULTAATTYPE (in de Catalogi API).
          type: string
          format: uri
          maxLength: 1000
        toelichting:
          description: Een toelichting op wat het resultaat van de zaak inhoudt.
          type: string
          maxLength: 1000
      required:
        - resultaattype
        - url
        - uuid
        - zaak
    ResultaatExpanded:
      allOf:
        - $ref: "#/components/schemas/Resultaat"
        - properties:
            _expand:
              $ref: "#/components/schemas/ResultaatEmbedded"
    ResultaatEmbedded:
      type: object
      properties:
        zaak:
          $ref: "#/components/schemas/ZaakExpanded"
        resultaattype:
          $ref: "https://raw.githubusercontent.com/VNG-Realisatie/gemma-zaken/master/api-specificatie/ztc/current_version/openapi.yaml#/components/schemas/ResultaatType"
    RolExpanded:
      allOf:
        - $ref: "#/components/schemas/RolBase"
        - properties:
            _expand:
              $ref: "#/components/schemas/RolEmbedded"
    RolEmbedded:
      type: object
      properties:
        zaak:
          example: {}
          $ref: "#/components/schemas/ZaakExpanded"
        roltype:
          $ref: "https://raw.githubusercontent.com/VNG-Realisatie/gemma-zaken/master/api-specificatie/ztc/current_version/openapi.yaml#/components/schemas/RolType"
        statussen:
          type: array
          items:
            $ref: "#/components/schemas/StatusExpanded"
    Status:
      type: object
      properties:
        url:
          description: |
            URL-referentie naar dit object. Dit is de unieke identificatie en locatie van dit object.
          type: string
          format: uri
          readOnly: true
          minLength: 1
          maxLength: 1000
        uuid:
          description: Unieke resource identifier (UUID4)
          type: string
          format: uuid
          readOnly: true
        zaak:
          description: URL-referentie naar de ZAAK.
          type: string
          format: uri
          minLength: 1
          maxLength: 1000
        statustype:
          description: URL-referentie naar het STATUSTYPE (in de Catalogi API).
          type: string
          format: uri
          maxLength: 1000
        datumStatusGezet:
          description: De datum waarop de ZAAK de status heeft verkregen.
          type: string
          format: date-time
        statustoelichting:
          description: |
            Een, voor de initiator van de zaak relevante, toelichting op de status van een zaak.
          type: string
          maxLength: 1000
        indicatieLaatstGezetteStatus:
          description: |
            Het gegeven is afleidbaar uit de historie van de attribuutsoort Datum status gezet van van alle statussen bij de desbetreffende zaak.
          type: boolean
          readOnly: true
        gezetdoor:
          description: |
            De BETROKKENE die in zijn/haar ROL in een ZAAK heeft geregistreerd dat STATUSsen in die ZAAK bereikt zijn.
          type: string
          format: uri
          maxLength: 200
        zaakinformatieobjecten:
          type: array
          items:
            title: ""
            type: string
            format: uri
            minLength: 1
            maxLength: 1000
          readOnly: true
          uniqueItems: true
      required:
        - datumStatusGezet
        - indicatieLaatstGezetteStatus
        - statustype
        - url
        - uuid
        - zaak
        - zaakinformatieobjecten
    StatusExpanded:
      allOf:
        - $ref: "#/components/schemas/Status"
        - properties:
            _expand:
              $ref: "#/components/schemas/StatusEmbedded"
    StatusEmbedded:
      type: object
      properties:
        statustype:
          $ref: "https://raw.githubusercontent.com/VNG-Realisatie/gemma-zaken/master/api-specificatie/ztc/current_version/openapi.yaml#/components/schemas/StatusType"
        gezetdoor:
          $ref: "#/components/schemas/RolVariant"
        zaakinformatieobjecten:
          type: array
          items:
            $ref: "#/components/schemas/ZaakInformatieObject"
    StatusRequestbody:
      type: object
      properties:
        url:
          description: |
            URL-referentie naar dit object. Dit is de unieke identificatie en locatie van dit object.
          type: string
          format: uri
          readOnly: true
          minLength: 1
          maxLength: 1000
        uuid:
          description: Unieke resource identifier (UUID4)
          type: string
          format: uuid
          readOnly: true
        zaak:
          description: URL-referentie naar de ZAAK.
          type: string
          format: uri
          minLength: 1
          maxLength: 1000
        statustype:
          description: URL-referentie naar het STATUSTYPE (in de Catalogi API).
          type: string
          format: uri
          maxLength: 1000
        datumStatusGezet:
          description: De datum waarop de ZAAK de status heeft verkregen.
          type: string
          format: date-time
        statustoelichting:
          description: |
            Een, voor de initiator van de zaak relevante, toelichting op de status van een zaak.
          type: string
          maxLength: 1000
        indicatieLaatstGezetteStatus:
          description: |
            Het gegeven is afleidbaar uit de historie van de attribuutsoort Datum status gezet van van alle statussen bij de desbetreffende zaak.
          type: boolean
          readOnly: true
        gezetdoor:
          description: |
            De BETROKKENE die in zijn/haar ROL in een ZAAK heeft geregistreerd dat STATUSsen in die ZAAK bereikt zijn.
          type: string
          format: uri
          maxLength: 200
      required:
        - datumStatusGezet
        - indicatieLaatstGezetteStatus
        - statustype
        - url
        - uuid
        - zaak
        - zaakinformatieobjecten
    SubVerblijfBuitenland:
      type: object
      properties:
        lndLandcode:
          description: |
            De code, behorende bij de landnaam, zoals opgenomen in de Land/Gebied-tabel van de BRP.
          type: string
          maxLength: 4
        lndLandnaam:
          description: De naam van het land, zoals opgenomen in de Land/Gebied-tabel
            van de BRP.
          type: string
          maxLength: 40
        subAdresBuitenland_1:
          type: string
          maxLength: 35
        subAdresBuitenland_2:
          type: string
          maxLength: 35
        subAdresBuitenland_3:
          type: string
          maxLength: 35
      required:
        - lndLandcode
        - lndLandnaam
    TerreinGebouwdObjectAdres:
      type: object
      properties:
        numIdentificatie:
          type: string
          maxLength: 100
        oaoIdentificatie:
          title: identificatie
          description: De unieke identificatie van het OBJECT
          type: string
          maxLength: 100
        wplWoonplaatsNaam:
          type: string
          maxLength: 80
        gorOpenbareRuimteNaam:
          description: |
            Een door het bevoegde gemeentelijke orgaan aan een OPENBARE RUIMTE toegekende benaming
          type: string
          maxLength: 80
        aoaPostcode:
          title: postcode
          type: string
          maxLength: 7
        aoaHuisnummer:
          title: huisnummer
          type: integer
          maximum: 99999
          minimum: 0
        aoaHuisletter:
          title: huisletter
          type: string
          maxLength: 1
        aoaHuisnummertoevoeging:
          title: huisnummertoevoeging
          type: string
          maxLength: 4
        ogoLocatieAanduiding:
          title: locatie aanduiding
          type: string
          maxLength: 100
      required:
        - aoaHuisnummer
        - gorOpenbareRuimteNaam
        - oaoIdentificatie
        - wplWoonplaatsNaam
    TypeWaterdeelEnum:
      description: |-
        Specificatie van het soort water
      type: string
      enum:
        - zee
        - waterloop
        - watervlakte
        - greppel_droge_sloot
      x-enumDescriptions:
        zee: Zee
        waterloop: Waterloop
        watervlakte: Watervlakte
        greppel_droge_sloot: Greppel, droge sloot
    ValidatieFout:
      description: Formaat van HTTP 4xx en 5xx fouten.
      type: object
      properties:
        title:
          description: Generieke titel voor het type fout
          type: string
        type:
          description: URI referentie naar het type fout, bedoeld voor developers
          type: string
        code:
          description: Systeemcode die het type fout aangeeft
          type: string
        status:
          description: De HTTP status code
          type: integer
        detail:
          description: Extra informatie bij de fout, indien beschikbaar
          type: string
        instance:
          description: |
            URI met referentie naar dit specifiek voorkomen van de fout. Deze kan gebruikt worden in combinatie met server logs, bijvoorbeeld.
          type: string
        invalidParams:
          type: array
          items:
            $ref: "#/components/schemas/FieldValidationError"
      required:
        - code
        - detail
        - instance
        - invalidParams
        - status
        - title
    VerblijfsAdres:
      type: object
      properties:
        aoaIdentificatie:
          title: identificatie
          description: De unieke identificatie van het OBJECT
          type: string
          maxLength: 100
        wplWoonplaatsNaam:
          type: string
          maxLength: 80
        gorOpenbareRuimteNaam:
          description: |
            Een door het bevoegde gemeentelijke orgaan aan een OPENBARE RUIMTE toegekende benaming
          type: string
          maxLength: 80
        aoaPostcode:
          title: postcode
          type: string
          maxLength: 7
        aoaHuisnummer:
          title: huisnummer
          type: integer
          maximum: 99999
          minimum: 0
        aoaHuisletter:
          title: huisletter
          type: string
          maxLength: 1
        aoaHuisnummertoevoeging:
          title: huisnummertoevoeging
          type: string
          maxLength: 4
        inpLocatiebeschrijving:
          title: locatie omschrijving
          type: string
          maxLength: 1000
      required:
        - aoaHuisnummer
        - aoaIdentificatie
        - gorOpenbareRuimteNaam
        - wplWoonplaatsNaam
    Verlenging:
      description: |
        Gegevens omtrent het verlengen van de doorlooptijd van de behandeling van de ZAAK
      type: object
      properties:
        reden:
          title: reden verlenging
          description: Omschrijving van de reden voor het verlengen van de behandeling
            van de zaak.
          type: string
          maxLength: 200
        duur:
          title: duur verlenging
          description: |
            Het aantal werkbare dagen waarmee de doorlooptijd van de behandeling van de ZAAK is verlengd (of verkort) ten opzichte van de eerder gecommuniceerde doorlooptijd.
          type: string
          format: duration
      required:
        - duur
        - reden
      nullable: true
    VertrouwelijkheidaanduidingEnum:
      title: Vertrouwlijkheidaanduiding
      description: |
        Aanduiding van de mate waarin het zaakdossier van de ZAAK voor de openbaarheid bestemd is. Optioneel - indien geen waarde gekozen wordt, dan wordt de waarde van het ZAAKTYPE overgenomen. Dit betekent dat de API _altijd_ een waarde teruggeeft.
      type: string
      enum:
        - openbaar
        - beperkt_openbaar
        - intern
        - zaakvertrouwelijk
        - vertrouwelijk
        - confidentieel
        - geheim
        - zeer_geheim
    Wijzigingen:
      type: object
      properties:
        oud:
          description: |
            Volledige JSON body van het object zoals dat bestond voordat de actie heeft plaatsgevonden.
          type: object
          additionalProperties: {}
        nieuw:
          description: Volledige JSON body van het object na de actie.
          type: object
          additionalProperties: {}
    WozObjectAdres:
      type: object
      properties:
        aoaIdentificatie:
          title: identificatie
          description: De unieke identificatie van het OBJECT
          type: string
          maxLength: 100
        wplWoonplaatsNaam:
          type: string
          maxLength: 80
        gorOpenbareRuimteNaam:
          description: |
            Een door het bevoegde gemeentelijke orgaan aan een OPENBARE RUIMTE toegekende benaming
          type: string
          maxLength: 80
        aoaPostcode:
          title: postcode
          type: string
          maxLength: 7
        aoaHuisnummer:
          title: huisnummer
          type: integer
          maximum: 99999
          minimum: 0
        aoaHuisletter:
          title: huisletter
          type: string
          maxLength: 1
        aoaHuisnummertoevoeging:
          title: huisnummertoevoeging
          type: string
          maxLength: 4
        locatieOmschrijving:
          type: string
          maxLength: 1000
      required:
        - aoaHuisnummer
        - aoaIdentificatie
        - gorOpenbareRuimteNaam
        - wplWoonplaatsNaam
    Zaak:
      description: |
        Set gegevensgroepdata from validated nested data.

        Usage: include the mixin on the ModelSerializer that has gegevensgroepen.
      type: object
      properties:
        url:
          description: |
            URL-referentie naar dit object. Dit is de unieke identificatie en locatie van dit object.
          type: string
          format: uri
          readOnly: true
          minLength: 1
          maxLength: 1000
        uuid:
          description: Unieke resource identifier (UUID4)
          type: string
          format: uuid
          readOnly: true
        identificatie:
          description: |
            De unieke identificatie van de ZAAK binnen de organisatie die verantwoordelijk is voor de behandeling van de ZAAK.
          type: string
          maxLength: 40
        bronorganisatie:
          description: |
            Het RSIN van de Niet-natuurlijk persoon zijnde de organisatie die de zaak heeft gecreeerd. Dit moet een geldig RSIN zijn van 9 nummers en voldoen aan https://nl.wikipedia.org/wiki/Burgerservicenummer#11-proef
          type: string
          maxLength: 9
        omschrijving:
          description: Een korte omschrijving van de zaak.
          type: string
          maxLength: 80
        toelichting:
          description: Een toelichting op de zaak.
          type: string
          maxLength: 1000
        zaaktype:
          description: |
            URL-referentie naar het ZAAKTYPE (in de Catalogi API) in de CATALOGUS waar deze voorkomt
          type: string
          format: uri
          maxLength: 1000
        registratiedatum:
          description: |
            De datum waarop de zaakbehandelende organisatie de ZAAK heeft geregistreerd. Indien deze niet opgegeven wordt, wordt de datum van vandaag gebruikt.
          type: string
          format: date
        verantwoordelijkeOrganisatie:
          description: |
            Het RSIN van de Niet-natuurlijk persoon zijnde de organisatie die eindverantwoordelijk is voor de behandeling van de zaak. Dit moet een geldig RSIN zijn van 9 nummers en voldoen aan https://nl.wikipedia.org/wiki/Burgerservicenummer#11-proef
          type: string
          maxLength: 9
        startdatum:
          description: De datum waarop met de uitvoering van de zaak is gestart
          type: string
          format: date
        einddatum:
          description: De datum waarop de uitvoering van de zaak afgerond is.
          type: string
          format: date
          readOnly: true
          nullable: true
        einddatumGepland:
          description: De datum waarop volgens de planning verwacht wordt dat de zaak
            afgerond wordt.
          type: string
          format: date
          nullable: true
        uiterlijkeEinddatumAfdoening:
          description: |
            De laatste datum waarop volgens wet- en regelgeving de zaak afgerond dient te zijn.
          type: string
          format: date
          nullable: true
        publicatiedatum:
          description: Datum waarop (het starten van) de zaak gepubliceerd is of wordt.
          type: string
          format: date
          nullable: true
        communicatiekanaal:
          description: |
            Het medium waarlangs de aanleiding om een zaak te starten is ontvangen. URL naar een communicatiekanaal in de VNG-Referentielijst van communicatiekanalen.
          type: string
          format: uri
          maxLength: 1000
        productenOfDiensten:
          description: |
            De producten en/of diensten die door de zaak worden voortgebracht. Dit zijn URLs naar de resources zoals die door de producten- en dienstencatalogus-API wordt ontsloten. De producten/diensten moeten bij het zaaktype vermeld zijn.
          type: array
          items:
            title: URL naar product/dienst
            type: string
            format: uri
            maxLength: 1000
        vertrouwelijkheidaanduiding:
          $ref: "#/components/schemas/VertrouwelijkheidaanduidingEnum"
        betalingsindicatie:
          oneOf:
            - $ref: "#/components/schemas/BetalingsindicatieEnum"
            - $ref: "#/components/schemas/BlankEnum"
          description: |
            Indicatie of de, met behandeling van de zaak gemoeide, kosten betaald zijn door de desbetreffende betrokkene.

            Uitleg bij mogelijke waarden:

            * `nvt` - Er is geen sprake van te betalen, met de zaak gemoeide, kosten.
            * `nog_niet` - De met de zaak gemoeide kosten zijn (nog) niet betaald.
            * `gedeeltelijk` - De met de zaak gemoeide kosten zijn gedeeltelijk betaald.
            * `geheel` - De met de zaak gemoeide kosten zijn geheel betaald.
        betalingsindicatieWeergave:
          description: Uitleg bij `betalingsindicatie`.
          type: string
          readOnly: true
        laatsteBetaaldatum:
          description: |
            De datum waarop de meest recente betaling is verwerkt van kosten die gemoeid zijn met behandeling van de zaak.
          type: string
          format: date-time
          nullable: true
        zaakgeometrie:
          allOf:
            - $ref: "#/components/schemas/GeoJSONGeometry"
          nullable: true
        verlenging:
          allOf:
            - $ref: "#/components/schemas/Verlenging"
          nullable: true
        opschorting:
          allOf:
            - $ref: "#/components/schemas/Opschorting"
          nullable: true
        selectielijstklasse:
          description: |
            URL-referentie naar de categorie in de gehanteerde 'Selectielijst Archiefbescheiden' die, gezien het zaaktype en het resultaattype van de zaak, bepalend is voor het archiefregime van de zaak.
          type: string
          format: uri
          maxLength: 1000
        hoofdzaak:
          title: Is deelzaak van
          description: |
            URL-referentie naar de ZAAK, waarom verzocht is door de initiator daarvan, die behandeld wordt in twee of meer separate ZAAKen waarvan de onderhavige ZAAK er één is.
          type: string
          format: uri
          nullable: true
          minLength: 1
          maxLength: 1000
        deelzaken:
          title: is deelzaak van
          description: URL-referenties naar deel ZAAKen.
          type: array
          items:
            title: ""
            type: string
            format: uri
          readOnly: true
          uniqueItems: true
        relevanteAndereZaken:
          title: zaak
          description: Een lijst van relevante andere zaken.
          type: array
          items:
            $ref: "#/components/schemas/RelevanteZaak"
        eigenschappen:
          title: zaak
          type: array
          items:
            title: ""
            type: string
            format: uri
          readOnly: true
          uniqueItems: true
        rollen:
          title: zaak
          type: array
          items:
            title: ""
            type: string
            format: uri
          readOnly: true
          uniqueItems: true
        status:
          description: Indien geen status bekend is, dan is de waarde 'null'
          type: string
          format: uri
          readOnly: true
          nullable: true
        zaakinformatieobjecten:
          title: zaak
          type: array
          items:
            title: ""
            type: string
            format: uri
          readOnly: true
          uniqueItems: true
        zaakobjecten:
          title: zaak
          type: array
          items:
            title: ""
            type: string
            format: uri
          readOnly: true
          uniqueItems: true
        kenmerken:
          title: zaak
          description: |
            Lijst van kenmerken. Merk op dat refereren naar gerelateerde objecten beter kan via `ZaakObject`.
          type: array
          items:
            $ref: "#/components/schemas/ZaakKenmerk"
        archiefnominatie:
          oneOf:
            - $ref: "#/components/schemas/ArchiefnominatieEnum"
            - $ref: "#/components/schemas/BlankEnum"
            - $ref: "#/components/schemas/NullEnum"
          description: |
            Aanduiding of het zaakdossier blijvend bewaard of na een bepaalde termijn vernietigd moet worden.

            Uitleg bij mogelijke waarden:

            * `blijvend_bewaren` - Het zaakdossier moet bewaard blijven en op de Archiefactiedatum overgedragen worden naar een archiefbewaarplaats.
            * `vernietigen` - Het zaakdossier moet op of na de Archiefactiedatum vernietigd worden.
          nullable: true
        archiefstatus:
          $ref: "#/components/schemas/ArchiefstatusEnum"
        archiefactiedatum:
          description: |
            De datum waarop het gearchiveerde zaakdossier vernietigd moet worden dan wel overgebracht moet worden naar een archiefbewaarplaats. Wordt automatisch berekend bij het aanmaken of wijzigen van een RESULTAAT aan deze ZAAK indien nog leeg.
          type: string
          format: date
          nullable: true
        resultaat:
          description: |
            URL-referentie naar het RESULTAAT. Indien geen resultaat bekend is, dan is de waarde 'null'
          type: string
          format: uri
          readOnly: true
          nullable: true
        opdrachtgevendeOrganisatie:
          description: |
            De krachtens publiekrecht ingestelde rechtspersoon dan wel ander niet-natuurlijk persoon waarbinnen het (bestuurs)orgaan zetelt dat opdracht heeft gegeven om taken uit te voeren waaraan de zaak invulling geeft.
          type: string
          maxLength: 9
        processobjectaard:
          title: Procesobjectaard
          description: |
            Omschrijving van het object, subject of gebeurtenis waarop, vanuit archiveringsoptiek, de zaak betrekking heeft.
          type: string
          nullable: true
          maxLength: 200
        resultaattoelichting:
          description: Een toelichting op wat het resultaat van de zaak inhoudt.
          type: string
          maxLength: 1000
        startdatumBewaartermijn:
          description: |
            De datum die de start markeert van de termijn waarop het zaakdossier vernietigd moet worden.
          type: string
          format: date
          nullable: true
        processobject:
          allOf:
            - $ref: "#/components/schemas/Processobject"
          nullable: true
      required:
        - betalingsindicatieWeergave
        - bronorganisatie
        - deelzaken
        - eigenschappen
        - einddatum
        - resultaat
        - rollen
        - startdatum
        - status
        - url
        - uuid
        - verantwoordelijkeOrganisatie
        - zaakinformatieobjecten
        - zaakobjecten
        - zaaktype
    ZaakExpanded:
      allOf:
        - $ref: "#/components/schemas/Zaak"
        - properties:
            _expand:
              $ref: "#/components/schemas/ZaakEmbedded"
    ZaakEmbedded:
      description: |
        <b>Let op</b>: de definities van onderstaande attributen kunnen recursie bevatten en worden mogelijk niet goed weergegeven in de gegenereerde voorbeeldberichten van Redoc of Swagger. Check altijd de yaml-specificitie voor correcte interpretatie en gebruik de referentie-implementatie om correcte responsberichten te genereren.
      type: object
      properties:
        zaaktype:
          $ref: "https://raw.githubusercontent.com/VNG-Realisatie/gemma-zaken/master/api-specificatie/ztc/current_version/openapi.yaml#/components/schemas/ZaakType"
        hoofdzaak:
          oneOf:
            - $ref: "#/components/schemas/ZaakExpanded"
            - $ref: "#/components/schemas/EmptyObject"
          example: {}
        deelzaken:
          type: array
          items:
            $ref: "#/components/schemas/ZaakExpanded"
          example:
            - {}
        relevanteAndereZaken:
          type: array
          items:
            $ref: "#/components/schemas/ZaakExpanded"
          example:
            - {}
        eigenschappen:
          type: array
          items:
            $ref: "#/components/schemas/ZaakEigenschap"
        rollen:
          type: array
          items:
            $ref: "#/components/schemas/RolExpanded"
        status:
          oneOf:
            - $ref: "#/components/schemas/StatusExpanded"
            - $ref: "#/components/schemas/EmptyObject"
        zaakobjecten:
          type: array
          items:
            $ref: "#/components/schemas/ZaakObjectVariant"
        resultaat:
          oneOf:
            - $ref: "#/components/schemas/ResultaatExpanded"
            - $ref: "#/components/schemas/EmptyObject"
    EmptyObject:
      description: |
        Leeg object. Dit wordt teruggeven indien het te expanderen veld een null waarde heeft.
      type: object
      example: {}
    ZaakBesluit:
      description: |
        A type of `ModelSerializer` that uses hyperlinked relationships with compound keys instead
        of primary key relationships.  Specifically:

        * A 'url' field is included instead of the 'id' field.
        * Relationships to other instances are hyperlinks, instead of primary keys.

        NOTE: this only works with DRF 3.1.0 and above.
      type: object
      properties:
        url:
          type: string
          format: uri
          readOnly: true
        uuid:
          description: Unieke resource identifier (UUID4)
          type: string
          format: uuid
          readOnly: true
        besluit:
          description: |
            URL-referentie naar het BESLUIT (in de Besluiten API), waar ook de relatieinformatie opgevraagd kan worden.
          type: string
          format: uri
          maxLength: 1000
      required:
        - besluit
        - url
        - uuid
    ZaakContactMoment:
      type: object
      properties:
        url:
          description: |
            URL-referentie naar dit object. Dit is de unieke identificatie en locatie van dit object.
          type: string
          format: uri
          readOnly: true
          minLength: 1
          maxLength: 1000
        uuid:
          description: Unieke resource identifier (UUID4)
          type: string
          format: uuid
          readOnly: true
        zaak:
          description: URL-referentie naar de ZAAK.
          type: string
          format: uri
          minLength: 1
          maxLength: 1000
        contactmoment:
          description: URL-referentie naar het CONTACTMOMENT (in de Klantinteractie
            API)
          type: string
          format: uri
          maxLength: 1000
      required:
        - contactmoment
        - url
        - uuid
        - zaak
    ZaakEigenschap:
      description: |
        A type of `ModelSerializer` that uses hyperlinked relationships with compound keys instead
        of primary key relationships.  Specifically:

        * A 'url' field is included instead of the 'id' field.
        * Relationships to other instances are hyperlinks, instead of primary keys.

        NOTE: this only works with DRF 3.1.0 and above.
      type: object
      properties:
        url:
          type: string
          format: uri
          readOnly: true
        uuid:
          description: Unieke resource identifier (UUID4)
          type: string
          format: uuid
          readOnly: true
        zaak:
          type: string
          format: uri
        eigenschap:
          description: URL-referentie naar de EIGENSCHAP (in de Catalogi API).
          type: string
          format: uri
          maxLength: 1000
        naam:
          description: De naam van de EIGENSCHAP (overgenomen uit de Catalogi API).
          type: string
          readOnly: true
        waarde:
          type: string
      required:
        - eigenschap
        - naam
        - url
        - uuid
        - waarde
        - zaak
    ZaakInformatieObject:
      type: object
      properties:
        url:
          description: |
            URL-referentie naar dit object. Dit is de unieke identificatie en locatie van dit object.
          type: string
          format: uri
          readOnly: true
          minLength: 1
          maxLength: 1000
        uuid:
          description: Unieke resource identifier (UUID4)
          type: string
          format: uuid
          readOnly: true
        informatieobject:
          description: |
            URL-referentie naar het INFORMATIEOBJECT (in de Documenten API), waar ook de relatieinformatie opgevraagd kan worden.
          type: string
          format: uri
          maxLength: 1000
        zaak:
          description: URL-referentie naar de ZAAK.
          type: string
          format: uri
          minLength: 1
          maxLength: 1000
        aardRelatieWeergave:
          allOf:
            - $ref: "#/components/schemas/AardRelatieWeergaveEnum"
          readOnly: true
        titel:
          description: De naam waaronder het INFORMATIEOBJECT binnen het OBJECT bekend
            is.
          type: string
          maxLength: 200
        beschrijving:
          description: Een op het object gerichte beschrijving van de inhoud vanhet
            INFORMATIEOBJECT.
          type: string
        registratiedatum:
          description: |
            De datum waarop de behandelende organisatie het INFORMATIEOBJECT heeft geregistreerd bij het OBJECT. Geldige waardes zijn datumtijden gelegen op of voor de huidige datum en tijd.
          type: string
          format: date-time
          readOnly: true
        vernietigingsdatum:
          description: De datum waarop het informatieobject uit het zaakdossier verwijderd
            moet worden.
          type: string
          format: date-time
          nullable: true
        status:
          description: |
            De bij de desbetreffende ZAAK behorende STATUS waarvoor het ZAAK-INFORMATIEOBJECT relevant is (geweest) met het oog op het bereiken van die STATUS en/of de communicatie daarover.
          type: string
          format: uri
          nullable: true
          minLength: 1
          maxLength: 1000
      required:
        - aardRelatieWeergave
        - informatieobject
        - registratiedatum
        - url
        - uuid
        - zaak
    ZaakInformatieObjectExpanded:
      allOf:
        - $ref: "#/components/schemas/ZaakInformatieObject"
        - properties:
            _expand:
              $ref: "#/components/schemas/ZaakInformatieObjectEmbedded"
    ZaakInformatieObjectEmbedded:
      type: object
      properties:
        status:
          $ref: "#/components/schemas/StatusExpanded"
    ZaakKenmerk:
      type: object
      properties:
        kenmerk:
          description: Identificeert uniek de zaak in een andere administratie.
          type: string
          maxLength: 40
        bron:
          description: De aanduiding van de administratie waar het kenmerk op slaat.
          type: string
          maxLength: 40
      required:
        - bron
        - kenmerk
    ZaakObjectExpanded:
      allOf:
        - $ref: "#/components/schemas/ZaakObjectBase"
        - properties:
            _expand:
              $ref: "#/components/schemas/ZaakObjectEmbedded"
    ZaakObjectEmbedded:
      type: object
      properties:
        zaakobjecttype:
          $ref: "https://raw.githubusercontent.com/VNG-Realisatie/gemma-zaken/master/api-specificatie/ztc/current_version/openapi.yaml#/components/schemas/ZaakObjectType"
    ZaakVerzoek:
      type: object
      properties:
        url:
          description: |
            URL-referentie naar dit object. Dit is de unieke identificatie en locatie van dit object.
          type: string
          format: uri
          readOnly: true
          minLength: 1
          maxLength: 1000
        uuid:
          description: Unieke resource identifier (UUID4)
          type: string
          format: uuid
          readOnly: true
        zaak:
          description: URL-referentie naar de ZAAK.
          type: string
          format: uri
          minLength: 1
          maxLength: 1000
        verzoek:
          description: URL-referentie naar het VERZOEK (in de Klantinteractie API)
          type: string
          format: uri
          maxLength: 1000
      required:
        - url
        - uuid
        - verzoek
        - zaak
    ZaakZoek:
      type: object
      properties:
        zaakgeometrie:
          $ref: "#/components/schemas/GeoWithin"
        uuid__in:
          description: Array of unieke resource identifiers (UUID4)
          type: array
          items:
            type: string
            format: uuid
          example:
            - "e80b7507-199a-484c-ad49-c41a1e43a6e7"
            - "78e12133-c467-4202-91ba-4417baa52801"
        identificatie:
          description: |
            De unieke identificatie van de ZAAK binnen de organisatie die verantwoordelijk is voor de behandeling van de ZAAK.
          type: string
          example: "zaak123"
        bronorganisatie:
          description: |
            Het RSIN van de Niet-natuurlijk persoon zijnde de organisatie die de zaak heeft gecreeerd. Dit moet een geldig RSIN zijn van 9 nummers en voldoen aan https://nl.wikipedia.org/wiki/Burgerservicenummer#11-proef
          type: string
          example: "000000000"
        bronorganisatie__in:
          description: Array van bronorganisaties.
          type: array
          items:
            type: string
            format: string
          example:
            - "000000000"
            - "479559740"
            - "875883461"
        zaaktype:
          description: |
            URL-referentie naar het ZAAKTYPE (in de Catalogi API) in de CATALOGUS waar deze voorkomt
          type: string
          example: "https://catalogi-api.test.vng.cloud/api/v1/zaaktypen/e80b7507-199a-484c-ad49-c41a1e43a6e7"
        zaaktype__in:
          description: Array van zaaktypen.
          type: array
          items:
            type: string
            format: uri
          example:
            - "https://catalogi-api.test.vng.cloud/api/v1/zaaktypen/e80b7507-199a-484c-ad49-c41a1e43a6e7"
            - "https://catalogi-api.test.vng.cloud/api/v1/zaaktypen/78e12133-c467-4202-91ba-4417baa52801"
        archiefnominatie:
          description: |
            Aanduiding of het zaakdossier blijvend bewaard of na een bepaalde termijn vernietigd moet worden. Uitleg bij mogelijke waarden: * `blijvend_bewaren` - Het zaakdossier moet bewaard blijven en op de Archiefactiedatum overgedragen worden naar een archiefbewaarplaats. * `vernietigen` - Het zaakdossier moet op of na de Archiefactiedatum vernietigd worden.
          type: string
          example: "blijvend_bewaren"
          enum:
            - blijvend_bewaren
            - vernietigen
        archiefnominatie__in:
          description: Multiple values may be separated by commas.
          type: array
          items:
            type: string
          example:
            - "blijvend_bewaren"
            - "vernietigen"
        archiefactiedatum:
          description: |
            De datum waarop het gearchiveerde zaakdossier vernietigd moet worden dan wel overgebracht moet worden naar een archiefbewaarplaats. Wordt automatisch berekend bij het aanmaken of wijzigen van een RESULTAAT aan deze ZAAK indien nog leeg.
          type: string
          example: "2019-01-02"
        archiefactiedatum__isnull:
          title: Archiefactiedatum is leeg
          type: boolean
          example: false
        archiefactiedatum__lt:
          title: Archiefactiedatum kleiner dan
          type: string
          example: "2020-01-02"
        archiefactiedatum__gt:
          title: Archiefactiedatum  groter dan
          type: string
          example: "2018-01-02"
        archiefstatus:
          description: |
            Aanduiding of het zaakdossier blijvend bewaard of na een bepaalde termijn vernietigd moet worden. Uitleg bij mogelijke waarden: * `nog_te_archiveren` - De zaak cq. het zaakdossier is nog niet als geheel gearchiveerd. * `gearchiveerd` - De zaak cq. het zaakdossier is als geheel niet-wijzigbaar bewaarbaar gemaakt. * `gearchiveerd_procestermijn_onbekend` - De zaak cq. het zaakdossier is als geheel niet-wijzigbaar bewaarbaar gemaakt maar de vernietigingsdatum kan nog niet bepaald worden. * `overgedragen` - De zaak cq. het zaakdossier is overgebracht naar een archiefbewaarplaats.
          type: string
          example: "nog_te_archiveren"
          enum:
            - nog_te_archiveren
            - gearchiveerd
            - gearchiveerd_procestermijn_onbekend
            - overgedragen
        archiefstatus__in:
          description: Multiple values may be separated by commas.
          type: array
          items:
            type: string
          example:
            - "nog_te_archiveren"
            - "gearchiveerd"
            - "gearchiveerd_procestermijn_onbekend"
            - "overgedragen"
        startdatum:
          description: De datum waarop met de uitvoering van de zaak is gestart
          type: string
          example: "2017-01-02"
        startdatum__gt:
          title: Startdatum groter dan
          type: string
          example: "2018-01-02"
        startdatum__gte:
          title: Startdatum groter dan of gelijk
          type: string
          example: "2016-01-02"
        startdatum__lt:
          title: Startdatum kleiner dan
          type: string
          example: "2018-01-02"
        startdatum__lte:
          title: Startdatum kleiner dan of gelijk
          type: string
          example: "2018-01-02"
        registratiedatum:
          description: |
            De datum waarop de zaakbehandelende organisatie de ZAAK heeft geregistreerd. Indien deze niet opgegeven wordt, wordt de datum van vandaag gebruikt.
          type: string
          example: "2015-01-02"
        registratiedatum__gt:
          title: Registratiedatum groter dan
          type: string
          example: "2014-01-02"
        registratiedatum__lt:
          title: Registratiedatum kleiner dan
          type: string
          example: "2016-01-02"
        einddatum:
          description: De datum waarop de uitvoering van de zaak afgerond is.
          type: string
          example: "2022-01-02"
        einddatum__isnull:
          title: Einddatum is leeg
          type: boolean
          example: false
        einddatum__gt:
          title: Einddatum groter dan
          type: string
          example: "2021-01-02"
        einddatum__lt:
          title: Einddatum kleiner dan
          type: string
          example: "2023-01-02"
          minLength: 1
        einddatumGepland:
          description: De datum waarop volgens de planning verwacht wordt dat de zaak
            afgerond wordt.
          type: string
          example: "2022-01-02"
        einddatumGepland__gt:
          title: Einddatum gepland groter dan
          type: string
          example: "2021-01-02"
        einddatumGepland__lt:
          title: Einddatum gepland kleiner dan
          type: string
          example: "2024-01-02"
        uiterlijkeEinddatumAfdoening:
          description: |
            De laatste datum waarop volgens wet- en regelgeving de zaak afgerond dient te zijn.
          type: string
          example: "2024-01-02"
        uiterlijkeEinddatumAfdoening__gt:
          title: Uiterlijke einddatumAfdoening groter dan
          type: string
          example: "2023-01-02"
        uiterlijkeEinddatumAfdoening__lt:
          title: Uiterlijke einddatumAfdoening kleiner dan
          type: string
          example: "2025-01-02"
        rol__betrokkeneType:
          description: |
            Type van de `betrokkene`. Uitleg bij mogelijke waarden: * `natuurlijk_persoon` - Natuurlijk persoon * `niet_natuurlijk_persoon` - Niet-natuurlijk persoon * `vestiging` - Vestiging * `organisatorische_eenheid` - Organisatorische eenheid * `medewerker` - Medewerker
          type: string
          example: "natuurlijk_persoon"
          enum:
            - natuurlijk_persoon
            - niet_natuurlijk_persoon
            - vestiging
            - organisatorische_eenheid
            - medewerker
        rol__betrokkene:
          description: URL-referentie naar een betrokkene gerelateerd aan de ZAAK.
          type: string
          example: "https://example.com"
        rol__omschrijvingGeneriek:
          description: Algemeen gehanteerde benaming van de aard van de ROL, afgeleid
            uit het ROLTYPE.
          type: string
          example: "initiator"
          enum:
            - adviseur
            - behandelaar
            - belanghebbende
            - beslisser
            - initiator
            - klantcontacter
            - zaakcoordinator
            - mede_initiator
        maximaleVertrouwelijkheidaanduiding:
          description: |
            Zaken met een vertrouwelijkheidaanduiding die beperkter is dan de aangegeven aanduiding worden uit de resultaten gefiltered.
          type: string
          example: "zaakvertrouwelijk"
          enum:
            - openbaar
            - beperkt_openbaar
            - intern
            - zaakvertrouwelijk
            - vertrouwelijk
            - confidentieel
            - geheim
            - zeer_geheim
        rol__betrokkeneIdentificatie__natuurlijkPersoon__inpBsn:
          description: |
            Het burgerservicenummer, bedoeld in artikel 1.1 van de Wet algemene bepalingen burgerservicenummer.
          type: string
          example: "123456789"
        rol__betrokkeneIdentificatie__natuurlijkPersoon__anpIdentificatie:
          description: Het door de gemeente uitgegeven unieke nummer voor een ANDER
            NATUURLIJK PERSOON
          type: string
          example: "123456789"
        rol__betrokkeneIdentificatie__natuurlijkPersoon__inpA_nummer:
          description: Het administratienummer van de persoon, bedoeld in de Wet BRP
          type: string
          example: "123456789"
        rol__betrokkeneIdentificatie__nietNatuurlijkPersoon__innNnpId:
          description: |
            Het door een kamer toegekend uniek nummer voor de INGESCHREVEN NIET-NATUURLIJK PERSOON
          type: string
          example: "123456789"
        rol__betrokkeneIdentificatie__nietNatuurlijkPersoon__annIdentificatie:
          description: |
            Het door de gemeente uitgegeven unieke nummer voor een ANDER NIET-NATUURLIJK PERSOON
          type: string
          example: "123456789"
        rol__betrokkeneIdentificatie__vestiging__vestigingsNummer:
          description: Een korte unieke aanduiding van de Vestiging.
          type: string
          example: "123456789"
        rol__betrokkeneIdentificatie__medewerker__identificatie:
          description: Een korte unieke aanduiding van de MEDEWERKER.
          type: string
          example: "123456789"
        rol__betrokkeneIdentificatie__organisatorischeEenheid__identificatie:
          description: Een korte identificatie van de organisatorische eenheid.
          type: string
          example: "123456789"
        ordering:
          description: |
            Het veld waarop de resultaten geordend worden. Het minnetje betekent omgekeerde volgorde.
          type: string
          example: "-startdatum"
          enum:
            - startdatum
            - -startdatum
            - einddatum
            - -einddatum
            - publicatiedatum
            - -publicatiedatum
            - archiefactiedatum
            - -archiefactiedatum
            - registratiedatum
            - -registratiedatum
            - identificatie
            - -identificatie
    ZakelijkRechtHeeftAlsGerechtigde:
      type: object
      properties:
        natuurlijkPersoon:
          $ref: "#/components/schemas/NatuurlijkPersoonIdentificatie"
        nietNatuurlijkPersoon:
          $ref: "#/components/schemas/NietNatuurlijkPersoonIdentificatie"
    PatchedZaakObjectVariant:
      allOf:
        - $ref: '#/components/schemas/ZaakObjectVariantBase'
      required:
        - objectType
    RolVariant:
      oneOf:
        - $ref: '#/components/schemas/MedewerkerRol'
        - $ref: '#/components/schemas/NatuurlijkPersoonRol'
        - $ref: '#/components/schemas/NietNatuurlijkPersoonRol'
        - $ref: '#/components/schemas/OrganisatorischeEenheidRol'
        - $ref: '#/components/schemas/VestigingRol'
    ZaakObjectVariant:
      allOf:
        - $ref: '#/components/schemas/ZaakObjectVariantBase'
      required:
        - objectType
        - url
        - uuid
        - zaak
      properties:
        _expand:
          $ref: "#/components/schemas/ZaakObjectEmbedded"
    AdresObject:
      type: object
      properties:
        identificatie:
          description: De unieke identificatie van het OBJECT
          type: string
          maxLength: 100
        wplWoonplaatsNaam:
          type: string
          maxLength: 80
        gorOpenbareRuimteNaam:
          description: |
            Een door het bevoegde gemeentelijke orgaan aan een OPENBARE RUIMTE toegekende benaming
          type: string
          maxLength: 80
        huisnummer:
          type: integer
          maximum: 99999
          minimum: 0
        huisletter:
          type: string
          maxLength: 1
        huisnummertoevoeging:
          type: string
          maxLength: 4
        postcode:
          type: string
          maxLength: 7
      required:
        - gorOpenbareRuimteNaam
        - huisnummer
        - identificatie
        - wplWoonplaatsNaam
    BuurtObject:
      type: object
      properties:
        buurtCode:
          description: De code behorende bij de naam van de buurt
          type: string
          maxLength: 2
        buurtNaam:
          description: De naam van de buurt, zoals die door het CBS wordt gebruikt.
          type: string
          maxLength: 40
        gemGemeenteCode:
          description: Een numerieke aanduiding waarmee een Nederlandse gemeente uniek
            wordt aangeduid
          type: string
          maxLength: 4
        wykWijkCode:
          description: De code behorende bij de naam van de wijk
          type: string
          maxLength: 2
      required:
        - buurtCode
        - buurtNaam
        - gemGemeenteCode
        - wykWijkCode
    GemeenteObject:
      type: object
      properties:
        gemeenteNaam:
          description: "De officiële door de gemeente vastgestelde gemeentenaam."
          type: string
          maxLength: 80
        gemeenteCode:
          description: Een numerieke aanduiding waarmee een Nederlandse gemeente uniek
            wordt aangeduid
          type: string
          maxLength: 4
      required:
        - gemeenteCode
        - gemeenteNaam
    GemeentelijkeOpenbareRuimteObject:
      type: object
      properties:
        identificatie:
          description: De unieke identificatie van het OBJECT
          type: string
          maxLength: 100
        openbareRuimteNaam:
          description: |
            Een door het bevoegde gemeentelijke orgaan aan een OPENBARE RUIMTE toegekende benaming
          type: string
          maxLength: 80
      required:
        - identificatie
        - openbareRuimteNaam
    HuishoudenObject:
      type: object
      properties:
        nummer:
          description: |
            Uniek identificerend administratienummer van een huishouden zoals toegekend door de gemeente waarin het huishouden woonachtig is.
          type: string
          maxLength: 12
        isGehuisvestIn:
          allOf:
            - $ref: "#/components/schemas/TerreinGebouwdObjectObject"
          nullable: true
      required:
        - nummer
    InrichtingselementObject:
      type: object
      properties:
        type:
          $ref: "#/components/schemas/InrichtingselementTypeEnum"
        identificatie:
          description: De unieke identificatie van het OBJECT
          type: string
          maxLength: 100
        naam:
          description: De benaming van het OBJECT
          type: string
          maxLength: 500
      required:
        - identificatie
        - type
    KadastraleOnroerendeZaakObject:
      type: object
      properties:
        kadastraleIdentificatie:
          description: De unieke identificatie van het OBJECT
          type: string
          maxLength: 100
        kadastraleAanduiding:
          description: |
            De typering van de kadastrale aanduiding van een onroerende zaak conform Kadaster
          type: string
          maxLength: 1000
      required:
        - kadastraleAanduiding
        - kadastraleIdentificatie
    KunstwerkdeelObject:
      type: object
      properties:
        type:
          $ref: "#/components/schemas/KunstwerkdeelTypeEnum"
        identificatie:
          description: De unieke identificatie van het OBJECT
          type: string
          maxLength: 100
        naam:
          type: string
          maxLength: 80
      required:
        - identificatie
        - naam
        - type
    MaatschappelijkeActiviteitObject:
      type: object
      properties:
        kvkNummer:
          description: |
            Landelijk uniek identificerend administratienummer van een MAATSCHAPPELIJKE ACTIVITEIT zoals toegewezen door de Kamer van Koophandel (KvK).
          type: string
          maxLength: 8
        handelsnaam:
          description: De naam waaronder de onderneming handelt.
          type: string
          maxLength: 200
      required:
        - handelsnaam
        - kvkNummer
    OpenbareRuimteObject:
      type: object
      properties:
        identificatie:
          description: De unieke identificatie van het OBJECT
          type: string
          maxLength: 100
        wplWoonplaatsNaam:
          type: string
          maxLength: 80
        gorOpenbareRuimteNaam:
          description: |
            Een door het bevoegde gemeentelijke orgaan aan een OPENBARE RUIMTE toegekende benaming
          type: string
          maxLength: 80
      required:
        - gorOpenbareRuimteNaam
        - identificatie
        - wplWoonplaatsNaam
    OverigeObject:
      type: object
      properties:
        overigeData:
          type: object
          additionalProperties: {}
      required:
        - overigeData
    PandObject:
      type: object
      properties:
        identificatie:
          description: De unieke identificatie van het OBJECT
          type: string
          maxLength: 100
      required:
        - identificatie
    SpoorbaandeelObject:
      type: object
      properties:
        type:
          $ref: "#/components/schemas/SpoorbaandeelTypeEnum"
        identificatie:
          description: De unieke identificatie van het OBJECT
          type: string
          maxLength: 100
        naam:
          description: De benaming van het OBJECT
          type: string
          maxLength: 500
      required:
        - identificatie
        - type
    TerreinGebouwdObjectObject:
      type: object
      properties:
        identificatie:
          description: De unieke identificatie van het OBJECT
          type: string
          maxLength: 100
        adresAanduidingGrp:
          allOf:
            - $ref: "#/components/schemas/TerreinGebouwdObjectAdres"
          nullable: true
      required:
        - identificatie
    TerreindeelObject:
      type: object
      properties:
        type:
          type: string
          maxLength: 40
        identificatie:
          description: De unieke identificatie van het OBJECT
          type: string
          maxLength: 100
        naam:
          description: De benaming van het OBJECT
          type: string
          maxLength: 500
      required:
        - identificatie
        - type
    TypeOverigeDefinitieObject:
      title: definitie object type overige
      description: |
        Verwijzing naar het schema van het type OBJECT als `objectType` de waarde "overige" heeft.

        * De URL referentie moet naar een JSON endpoint   wijzen waarin het objecttype gedefinieerd is, inclusief het   [JSON-schema](https://json-schema.org/).
        * Gebruik het `schema` attribuut om te verwijzen naar het schema binnen   de objecttype resource (deze gebruikt het   [jq](http://stedolan.github.io/jq/) formaat.
        * Gebruik het `objectData` attribuut om te verwijzen naar de gegevens   binnen het OBJECT. Deze gebruikt ook het   [jq](http://stedolan.github.io/jq/) formaat.

        Indien je hier gebruikt van maakt, dan moet je een OBJECT url opgeven en is het gebruik van objectIdentificatie niet mogelijk. De opgegeven OBJECT url wordt gevalideerd tegen het schema van het opgegeven objecttype.
      type: object
      properties:
        url:
          title: Objecttype-URL
          description: |
            URL-referentie naar de objecttype resource in een API. Deze resource moet de [JSON-schema](https://json-schema.org/)-definitie van het objecttype bevatten.
          type: string
          format: uri
          maxLength: 1000
        schema:
          title: schema-pad
          description: |
            Een geldige [jq](http://stedolan.github.io/jq/) expressie. Dit wordt gecombineerd met de resource uit het `url`-attribuut om het schema van het objecttype uit te lezen. Bijvoorbeeld: `.jsonSchema`.
          type: string
          maxLength: 100
        objectData:
          title: objectgegevens-pad
          description: |
            Een geldige [jq](http://stedolan.github.io/jq/) expressie. Dit wordt gecombineerd met de JSON data uit de OBJECT url om de objectgegevens uit te lezen en de vorm van de gegevens tegen het schema te valideren. Bijvoorbeeld: `.record.data`.
          type: string
          maxLength: 100
      required:
        - objectData
        - schema
        - url
    WaterdeelObject:
      type: object
      properties:
        typeWaterdeel:
          $ref: "#/components/schemas/TypeWaterdeelEnum"
        identificatie:
          description: De unieke identificatie van het OBJECT
          type: string
          maxLength: 100
        naam:
          description: De benaming van het OBJECT
          type: string
          maxLength: 500
      required:
        - identificatie
        - typeWaterdeel
    WegdeelObject:
      type: object
      properties:
        type:
          type: string
          maxLength: 100
        identificatie:
          description: De unieke identificatie van het OBJECT
          type: string
          maxLength: 100
        naam:
          description: De benaming van het OBJECT
          type: string
          maxLength: 500
      required:
        - identificatie
        - type
    WijkObject:
      type: object
      properties:
        wijkCode:
          description: De code behorende bij de naam van de wijk.
          type: string
          maxLength: 2
        wijkNaam:
          description: De naam van de wijk, zoals die door het CBS wordt gebruikt.
          type: string
          maxLength: 40
        gemGemeenteCode:
          description: Een numerieke aanduiding waarmee een Nederlandse gemeente uniek
            wordt aangeduid
          type: string
          maxLength: 4
      required:
        - gemGemeenteCode
        - wijkCode
        - wijkNaam
    WoonplaatsObject:
      type: object
      properties:
        identificatie:
          description: De unieke identificatie van het OBJECT
          type: string
          maxLength: 100
        woonplaatsNaam:
          description: |
            De door het bevoegde gemeentelijke orgaan aan een WOONPLAATS toegekende benaming.
          type: string
          maxLength: 80
      required:
        - identificatie
        - woonplaatsNaam
    WozDeelobjectObject:
      type: object
      properties:
        nummerWozDeelObject:
          description: Uniek identificatienummer voor het deelobject binnen een WOZ-object.
          type: string
          maxLength: 6
        isOnderdeelVan:
          $ref: "#/components/schemas/WozObjectObject"
      required:
        - nummerWozDeelObject
    WozObjectObject:
      type: object
      properties:
        wozObjectNummer:
          description: De unieke identificatie van het OBJECT
          type: string
          maxLength: 100
        aanduidingWozObject:
          allOf:
            - $ref: "#/components/schemas/WozObjectAdres"
          nullable: true
      required:
        - wozObjectNummer
    WozWaardeObject:
      type: object
      properties:
        waardepeildatum:
          description: De datum waarnaar de waarde van het WOZ-object wordt bepaald.
          type: string
          maxLength: 9
        isVoor:
          $ref: "#/components/schemas/WozObjectObject"
      required:
        - waardepeildatum
    ZakelijkRechtObject:
      type: object
      properties:
        identificatie:
          description: De unieke identificatie van het OBJECT
          type: string
          maxLength: 100
        avgAard:
          description: aanduiding voor de aard van het recht
          type: string
          maxLength: 1000
        heeftBetrekkingOp:
          $ref: "#/components/schemas/KadastraleOnroerendeZaakObject"
        heeftAlsGerechtigde:
          $ref: "#/components/schemas/ZakelijkRechtHeeftAlsGerechtigde"
      required:
        - avgAard
        - identificatie
    InrichtingselementTypeEnum:
      description: |-
        Specificatie van de aard van het inrichtingselement.
      type: string
      enum:
        - bak
        - bord
        - installatie
        - kast
        - mast
        - paal
        - sensor
        - straatmeubilair
        - waterinrichtingselement
        - weginrichtingselement
      x-enumDescriptions:
        bak: Bak
        bord: Bord
        installatie: Installatie
        kast: Kast
        mast: Mast
        paal: Paal
        sensor: Sensor
        straatmeubilair: Straatmeubilair
        waterinrichtingselement: Waterinrichtingselement
        weginrichtingselement: Weginrichtingselement
    KunstwerkdeelTypeEnum:
      description: |-
        Specificatie van het soort Kunstwerk waartoe het kunstwerkdeel behoort.
      type: string
      enum:
        - keermuur
        - overkluizing
        - duiker
        - faunavoorziening
        - vispassage
        - bodemval
        - coupure
        - ponton
        - voorde
        - hoogspanningsmast
        - gemaal
        - perron
        - sluis
        - strekdam
        - steiger
        - stuw
      x-enumDescriptions:
        keermuur: Keermuur
        overkluizing: Overkluizing
        duiker: Duiker
        faunavoorziening: Faunavoorziening
        vispassage: Vispassage
        bodemval: Bodemval
        coupure: Coupure
        ponton: Ponton
        voorde: Voorde
        hoogspanningsmast: Hoogspanningsmast
        gemaal: Gemaal
        perron: Perron
        sluis: Sluis
        strekdam: Strekdam
        steiger: Steiger
        stuw: Stuw
    SpoorbaandeelTypeEnum:
      description: |-
        Specificatie van het soort Spoorbaan
      type: string
      enum:
        - breedspoor
        - normaalspoor
        - smalspoor
        - spoorbaan
      x-enumDescriptions:
        breedspoor: breedspoor
        normaalspoor: normaalspoor
        smalspoor: smalspoor
        spoorbaan: spoorbaan
    RolBase:
      type: object
      properties:
        url:
          description: |
            URL-referentie naar dit object. Dit is de unieke identificatie en locatie van dit object.
          type: string
          format: uri
          readOnly: true
          minLength: 1
          maxLength: 1000
        uuid:
          description: Unieke resource identifier (UUID4)
          type: string
          format: uuid
          readOnly: true
        zaak:
          description: URL-referentie naar de ZAAK.
          type: string
          format: uri
          minLength: 1
          maxLength: 1000
        betrokkene:
          description: URL-referentie naar een betrokkene gerelateerd aan de ZAAK.
          type: string
          format: uri
          maxLength: 1000
        betrokkeneType:
          $ref: "#/components/schemas/BetrokkeneTypeEnum"
        afwijkendeNaamBetrokkene:
          description: |
            De naam van de betrokkene waaronder deze in relatie tot de zaak aangesproken wil worden.
          type: string
          maxLength: 625
        roltype:
          description: URL-referentie naar een roltype binnen het ZAAKTYPE van de
            ZAAK.
          type: string
          format: uri
          maxLength: 1000
        omschrijving:
          description: Omschrijving van de aard van de ROL, afgeleid uit het ROLTYPE.
          type: string
          readOnly: true
        omschrijvingGeneriek:
          description: |
            Algemeen gehanteerde benaming van de aard van de ROL, afgeleid uit het ROLTYPE.

            Uitleg bij mogelijke waarden:

            * `adviseur` - (Adviseur) Kennis in dienst stellen van de behandeling van (een deel van) een zaak.
            * `behandelaar` - (Behandelaar) De vakinhoudelijke behandeling doen van (een deel van) een zaak.
            * `belanghebbende` - (Belanghebbende) Vanuit eigen en objectief belang rechtstreeks betrokken zijn bij de behandeling en/of de uitkomst van een zaak.
            * `beslisser` - (Beslisser) Nemen van besluiten die voor de uitkomst van een zaak noodzakelijk zijn.
            * `initiator` - (Initiator) Aanleiding geven tot de start van een zaak ..
            * `klantcontacter` - (Klantcontacter) Het eerste aanspreekpunt zijn voor vragen van burgers en bedrijven ..
            * `zaakcoordinator` - (Zaakcoördinator) Er voor zorg dragen dat de behandeling van de zaak in samenhang uitgevoerd wordt conform de daarover gemaakte afspraken.
            * `mede_initiator` - Mede-initiator
          type: string
          readOnly: true
        roltoelichting:
          type: string
          maxLength: 1000
        registratiedatum:
          description: De datum waarop dit object is geregistreerd.
          type: string
          format: date-time
          readOnly: true
        indicatieMachtiging:
          oneOf:
            - $ref: "#/components/schemas/IndicatieMachtigingEnum"
            - $ref: "#/components/schemas/BlankEnum"
          description: |
            Indicatie machtiging

            Uitleg bij mogelijke waarden:

            * `gemachtigde` - De betrokkene in de rol bij de zaak is door een andere betrokkene bij dezelfde zaak gemachtigd om namens hem of haar te handelen
            * `machtiginggever` - De betrokkene in de rol bij de zaak heeft een andere betrokkene bij dezelfde zaak gemachtigd om namens hem of haar te handelen
        contactpersoonRol:
          allOf:
            - $ref: "#/components/schemas/ContactPersoonRol"
          nullable: true
        statussen:
          description: |
            De BETROKKENE die in zijn/haar ROL in een ZAAK heeft geregistreerd dat STATUSsen in die ZAAK bereikt zijn.
          type: array
          items:
            title: ""
            type: string
            format: uri
            minLength: 1
            maxLength: 1000
          readOnly: true
          uniqueItems: true
        _expand:
          $ref: "#/components/schemas/RolEmbedded"
      required:
        - betrokkeneType
        - omschrijving
        - omschrijvingGeneriek
        - registratiedatum
        - roltoelichting
        - roltype
        - statussen
        - url
        - uuid
        - zaak
      discriminator:
        propertyName: betrokkeneType
        mapping:
          medewerker: "#/components/schemas/MedewerkerRol"
          natuurlijk_persoon: "#/components/schemas/NatuurlijkPersoonRol"
          niet_natuurlijk_persoon: "#/components/schemas/NietNatuurlijkPersoonRol"
          organisatorische_eenheid: "#/components/schemas/OrganisatorischeEenheidRol"
          vestiging: "#/components/schemas/VestigingRol"
    ZaakObjectBase:
      type: object
      properties:
        url:
          description: |
            URL-referentie naar dit object. Dit is de unieke identificatie en locatie van dit object.
          type: string
          format: uri
          readOnly: true
          minLength: 1
          maxLength: 1000
        uuid:
          description: Unieke resource identifier (UUID4)
          type: string
          format: uuid
          readOnly: true
        zaak:
          description: URL-referentie naar de ZAAK.
          type: string
          format: uri
          minLength: 1
          maxLength: 1000
        object:
          description: URL-referentie naar de resource die het OBJECT beschrijft.
          type: string
          format: uri
          maxLength: 1000
        zaakobjecttype:
          description: URL-referentie naar het ZAAKOBJECTTYPE (in de Catalogi API).
          type: string
          format: uri
          maxLength: 1000
        objectType:
          $ref: "#/components/schemas/ObjectTypeEnum"
        objectTypeOverige:
          description: Beschrijft het type OBJECT als `objectType` de waarde "overige"
            heeft.
          type: string
          pattern: '[a-z\_]+'
          maxLength: 100
        objectTypeOverigeDefinitie:
          allOf:
            - $ref: "#/components/schemas/TypeOverigeDefinitieObject"
          nullable: true
        relatieomschrijving:
          description: Omschrijving van de betrekking tussen de ZAAK en het OBJECT.
          type: string
          maxLength: 80
      discriminator:
        propertyName: objectType
        mapping:
          adres: "#/components/schemas/AdresZaakObject"
          besluit: "#/components/schemas/BesluitZaakObject"
          buurt: "#/components/schemas/BuurtZaakObject"
          enkelvoudig_document: "#/components/schemas/EnkelvoudigDocumentZaakObject"
          gemeente: "#/components/schemas/GemeenteZaakObject"
          gemeentelijke_openbare_ruimte: "#/components/schemas/GemeentelijkeOpenbareRuimteZaakObject"
          huishouden: "#/components/schemas/HuishoudenZaakObject"
          inrichtingselement: "#/components/schemas/InrichtingselementZaakObject"
          kadastrale_onroerende_zaak: "#/components/schemas/KadastraleOnroerendeZaakZaakObject"
          kunstwerkdeel: "#/components/schemas/KunstwerkdeelZaakObject"
          maatschappelijke_activiteit: "#/components/schemas/MaatschappelijkeActiviteitZaakObject"
          medewerker: "#/components/schemas/MedewerkerZaakObject"
          natuurlijk_persoon: "#/components/schemas/NatuurlijkPersoonZaakObject"
          niet_natuurlijk_persoon: "#/components/schemas/NietNatuurlijkPersoonZaakObject"
          openbare_ruimte: "#/components/schemas/OpenbareRuimteZaakObject"
          organisatorische_eenheid: "#/components/schemas/OrganisatorischeEenheidZaakObject"
          pand: "#/components/schemas/PandZaakObject"
          spoorbaandeel: "#/components/schemas/SpoorbaandeelZaakObject"
          status: "#/components/schemas/StatusZaakObject"
          terreindeel: "#/components/schemas/TerreindeelZaakObject"
          terrein_gebouwd_object: "#/components/schemas/TerreinGebouwdObjectZaakObject"
          vestiging: "#/components/schemas/VestigingZaakObject"
          waterdeel: "#/components/schemas/WaterdeelZaakObject"
          wegdeel: "#/components/schemas/WegdeelZaakObject"
          wijk: "#/components/schemas/WijkZaakObject"
          woonplaats: "#/components/schemas/WoonplaatsZaakObject"
          woz_deelobject: "#/components/schemas/WozDeelobjectZaakObject"
          woz_object: "#/components/schemas/WozObjectZaakObject"
          woz_waarde: "#/components/schemas/WozWaardeZaakObject"
          zakelijk_recht: "#/components/schemas/ZakelijkRechtZaakObject"
          overige: "#/components/schemas/OverigeZaakObject"
    MedewerkerIdentificatie:
      type: object
      properties:
        identificatie:
          description: Een korte unieke aanduiding van de MEDEWERKER.
          type: string
          maxLength: 24
        achternaam:
          description: De achternaam zoals de MEDEWERKER die in het dagelijkse verkeer
            gebruikt.
          type: string
          maxLength: 200
        voorletters:
          description: |
            De verzameling letters die gevormd wordt door de eerste letter van alle in volgorde voorkomende voornamen.
          type: string
          maxLength: 20
        voorvoegselAchternaam:
          description: |
            Dat deel van de geslachtsnaam dat voorkomt in Tabel 36 (GBA), voorvoegseltabel, en door een spatie van de geslachtsnaam is
          type: string
          maxLength: 10
    NatuurlijkPersoonIdentificatie:
      type: object
      properties:
        inpBsn:
          description: |
            Het burgerservicenummer, bedoeld in artikel 1.1 van de Wet algemene bepalingen burgerservicenummer.
          type: string
          maxLength: 9
        anpIdentificatie:
          description: Het door de gemeente uitgegeven unieke nummer voor een ANDER
            NATUURLIJK PERSOON
          type: string
          maxLength: 17
        inpA_nummer:
          description: Het administratienummer van de persoon, bedoeld in de Wet BRP
          type: string
          pattern: ^[1-9][0-9]{9}$
          maxLength: 10
        geslachtsnaam:
          description: De stam van de geslachtsnaam.
          type: string
          maxLength: 200
        voorvoegselGeslachtsnaam:
          type: string
          maxLength: 80
        voorletters:
          description: |
            De verzameling letters die gevormd wordt door de eerste letter van alle in volgorde voorkomende voornamen.
          type: string
          maxLength: 20
        voornamen:
          description: Voornamen bij de naam die de persoon wenst te voeren.
          type: string
          maxLength: 200
        geslachtsaanduiding:
          oneOf:
            - $ref: "#/components/schemas/GeslachtsaanduidingEnum"
            - $ref: "#/components/schemas/BlankEnum"
          description: |
            Een aanduiding die aangeeft of de persoon een man of een vrouw is, of dat het geslacht nog onbekend is.

            Uitleg bij mogelijke waarden:

            * `m` - Man
            * `v` - Vrouw
            * `o` - Onbekend
        geboortedatum:
          type: string
          maxLength: 18
        verblijfsadres:
          allOf:
            - $ref: "#/components/schemas/VerblijfsAdres"
          nullable: true
        subVerblijfBuitenland:
          allOf:
            - $ref: "#/components/schemas/SubVerblijfBuitenland"
          nullable: true
    NietNatuurlijkPersoonIdentificatie:
      type: object
      properties:
        innNnpId:
          description: |
            Het door een kamer toegekend uniek nummer voor de INGESCHREVEN NIET-NATUURLIJK PERSOON
          type: string
          maxLength: 9
        annIdentificatie:
          description: |
            Het door de gemeente uitgegeven unieke nummer voor een ANDER NIET-NATUURLIJK PERSOON
          type: string
          maxLength: 17
        statutaireNaam:
          description: |
            Naam van de niet-natuurlijke persoon zoals deze is vastgelegd in de statuten (rechtspersoon) of in de vennootschapsovereenkomst is overeengekomen (Vennootschap onder firma of Commanditaire vennootschap).
          type: string
          maxLength: 500
        innRechtsvorm:
          oneOf:
            - $ref: "#/components/schemas/InnRechtsvormEnum"
            - $ref: "#/components/schemas/BlankEnum"
          description: De juridische vorm van de NIET-NATUURLIJK PERSOON.
        bezoekadres:
          description: De gegevens over het adres van de NIET-NATUURLIJK PERSOON
          type: string
          maxLength: 1000
        subVerblijfBuitenland:
          allOf:
            - $ref: "#/components/schemas/SubVerblijfBuitenland"
          nullable: true
    OrganisatorischeEenheidIdentificatie:
      type: object
      properties:
        identificatie:
          description: Een korte identificatie van de organisatorische eenheid.
          type: string
          maxLength: 24
        naam:
          description: De feitelijke naam van de organisatorische eenheid.
          type: string
          maxLength: 50
        isGehuisvestIn:
          type: string
          maxLength: 24
    VestigingIdentificatie:
      type: object
      properties:
        vestigingsNummer:
          description: Een korte unieke aanduiding van de Vestiging.
          type: string
          maxLength: 24
        handelsnaam:
          description: De naam van de vestiging waaronder gehandeld wordt.
          type: array
          items:
            type: string
            maxLength: 625
        verblijfsadres:
          allOf:
            - $ref: "#/components/schemas/VerblijfsAdres"
          nullable: true
        subVerblijfBuitenland:
          allOf:
            - $ref: "#/components/schemas/SubVerblijfBuitenland"
          nullable: true
        kvkNummer:
          description: Een uniek nummer gekoppeld aan de onderneming.
          type: string
          maxLength: 8
    MedewerkerRol:
      allOf:
        - $ref: "#/components/schemas/RolBase"
      properties:
        betrokkeneType:
          enum:
            - medewerker
        betrokkeneIdentificatie:
          $ref: "#/components/schemas/MedewerkerIdentificatie"
    NatuurlijkPersoonRol:
      allOf:
        - $ref: "#/components/schemas/RolBase"
      properties:
        betrokkeneType:
          enum:
            - natuurlijk_persoon
        betrokkeneIdentificatie:
          $ref: "#/components/schemas/NatuurlijkPersoonIdentificatie"
    NietNatuurlijkPersoonRol:
      allOf:
        - $ref: "#/components/schemas/RolBase"
      properties:
        betrokkeneType:
          enum:
            - niet_natuurlijk_persoon
        betrokkeneIdentificatie:
          $ref: "#/components/schemas/NietNatuurlijkPersoonIdentificatie"
    OrganisatorischeEenheidRol:
      allOf:
        - $ref: "#/components/schemas/RolBase"
      properties:
        betrokkeneType:
          enum:
            - organisatorische_eenheid
        betrokkeneIdentificatie:
          $ref: "#/components/schemas/OrganisatorischeEenheidIdentificatie"
    VestigingRol:
      allOf:
        - $ref: "#/components/schemas/RolBase"
      properties:
        betrokkeneType:
          enum:
            - vestiging
        betrokkeneIdentificatie:
          $ref: "#/components/schemas/VestigingIdentificatie"
    AdresZaakObject:
      allOf:
        - $ref: "#/components/schemas/ZaakObjectBase"
      properties:
        objectType:
          enum:
            - adres
        objectIdentificatie:
          $ref: "#/components/schemas/AdresObject"
    BesluitZaakObject:
      allOf:
        - $ref: "#/components/schemas/ZaakObjectBase"
      properties:
        objectType:
          enum:
            - besluit
    BuurtZaakObject:
      allOf:
        - $ref: "#/components/schemas/ZaakObjectBase"
      properties:
        objectType:
          enum:
            - buurt
        objectIdentificatie:
          $ref: "#/components/schemas/BuurtObject"
    EnkelvoudigDocumentZaakObject:
      allOf:
        - $ref: "#/components/schemas/ZaakObjectBase"
      properties:
        objectType:
          enum:
            - enkelvoudig_document
    GemeenteZaakObject:
      allOf:
        - $ref: "#/components/schemas/ZaakObjectBase"
      properties:
        objectType:
          enum:
            - gemeente
        objectIdentificatie:
          $ref: "#/components/schemas/GemeenteObject"
    GemeentelijkeOpenbareRuimteZaakObject:
      allOf:
        - $ref: "#/components/schemas/ZaakObjectBase"
      properties:
        objectType:
          enum:
            - gemeentelijke_openbare_ruimte
        objectIdentificatie:
          $ref: "#/components/schemas/GemeentelijkeOpenbareRuimteObject"
    HuishoudenZaakObject:
      allOf:
        - $ref: "#/components/schemas/ZaakObjectBase"
      properties:
        objectType:
          enum:
            - huishouden
        objectIdentificatie:
          $ref: "#/components/schemas/HuishoudenObject"
    InrichtingselementZaakObject:
      allOf:
        - $ref: "#/components/schemas/ZaakObjectBase"
      properties:
        objectType:
          enum:
            - inrichtingselement
        objectIdentificatie:
          $ref: "#/components/schemas/InrichtingselementObject"
    KadastraleOnroerendeZaakZaakObject:
      allOf:
        - $ref: "#/components/schemas/ZaakObjectBase"
      properties:
        objectType:
          enum:
            - kadastrale_onroerende_zaak
        objectIdentificatie:
          $ref: "#/components/schemas/KadastraleOnroerendeZaakObject"
    KunstwerkdeelZaakObject:
      allOf:
        - $ref: "#/components/schemas/ZaakObjectBase"
      properties:
        objectType:
          enum:
            - kunstwerkdeel
        objectIdentificatie:
          $ref: "#/components/schemas/KunstwerkdeelObject"
    MaatschappelijkeActiviteitZaakObject:
      allOf:
        - $ref: "#/components/schemas/ZaakObjectBase"
      properties:
        objectType:
          enum:
            - maatschappelijke_activiteit
        objectIdentificatie:
          $ref: "#/components/schemas/MaatschappelijkeActiviteitObject"
    MedewerkerZaakObject:
      allOf:
        - $ref: "#/components/schemas/ZaakObjectBase"
      properties:
        objectType:
          enum:
            - medewerker
        betrokkeneIdentificatie:
          $ref: "#/components/schemas/MedewerkerIdentificatie"
    NatuurlijkPersoonZaakObject:
      allOf:
        - $ref: "#/components/schemas/ZaakObjectBase"
      properties:
        objectType:
          enum:
            - natuurlijk_persoon
        betrokkeneIdentificatie:
          $ref: "#/components/schemas/NatuurlijkPersoonIdentificatie"
    NietNatuurlijkPersoonZaakObject:
      allOf:
        - $ref: "#/components/schemas/ZaakObjectBase"
      properties:
        objectType:
          enum:
            - niet_natuurlijk_persoon
        betrokkeneIdentificatie:
          $ref: "#/components/schemas/NietNatuurlijkPersoonIdentificatie"
    OpenbareRuimteZaakObject:
      allOf:
        - $ref: "#/components/schemas/ZaakObjectBase"
      properties:
        objectType:
          enum:
            - openbare_ruimte
        objectIdentificatie:
          $ref: "#/components/schemas/OpenbareRuimteObject"
    OrganisatorischeEenheidZaakObject:
      allOf:
        - $ref: "#/components/schemas/ZaakObjectBase"
      properties:
        objectType:
          enum:
            - organisatorische_eenheid
        betrokkeneIdentificatie:
          $ref: "#/components/schemas/OrganisatorischeEenheidIdentificatie"
    PandZaakObject:
      allOf:
        - $ref: "#/components/schemas/ZaakObjectBase"
      properties:
        objectType:
          enum:
            - pand
        objectIdentificatie:
          $ref: "#/components/schemas/PandObject"
    SpoorbaandeelZaakObject:
      allOf:
        - $ref: "#/components/schemas/ZaakObjectBase"
      properties:
        objectType:
          enum:
            - spoorbaandeel
        objectIdentificatie:
          $ref: "#/components/schemas/SpoorbaandeelObject"
    StatusZaakObject:
      allOf:
        - $ref: "#/components/schemas/ZaakObjectBase"
      properties:
        objectType:
          enum:
            - status
    TerreindeelZaakObject:
      allOf:
        - $ref: "#/components/schemas/ZaakObjectBase"
      properties:
        objectType:
          enum:
            - terreindeel
        objectIdentificatie:
          $ref: "#/components/schemas/TerreindeelObject"
    TerreinGebouwdObjectZaakObject:
      allOf:
        - $ref: "#/components/schemas/ZaakObjectBase"
      properties:
        objectType:
          enum:
            - terrein_gebouwd_object
        objectIdentificatie:
          $ref: "#/components/schemas/TerreinGebouwdObjectObject"
    VestigingZaakObject:
      allOf:
        - $ref: "#/components/schemas/ZaakObjectBase"
      properties:
        objectType:
          enum:
            - vestiging
        betrokkeneIdentificatie:
          $ref: "#/components/schemas/VestigingIdentificatie"
    WaterdeelZaakObject:
      allOf:
        - $ref: "#/components/schemas/ZaakObjectBase"
      properties:
        objectType:
          enum:
            - waterdeel
        objectIdentificatie:
          $ref: "#/components/schemas/WaterdeelObject"
    WegdeelZaakObject:
      allOf:
        - $ref: "#/components/schemas/ZaakObjectBase"
      properties:
        objectType:
          enum:
            - wegdeel
        objectIdentificatie:
          $ref: "#/components/schemas/WegdeelObject"
    WijkZaakObject:
      allOf:
        - $ref: "#/components/schemas/ZaakObjectBase"
      properties:
        objectType:
          enum:
            - wijk
        objectIdentificatie:
          $ref: "#/components/schemas/WijkObject"
    WoonplaatsZaakObject:
      allOf:
        - $ref: "#/components/schemas/ZaakObjectBase"
      properties:
        objectType:
          enum:
            - woonplaats
        objectIdentificatie:
          $ref: "#/components/schemas/WoonplaatsObject"
    WozDeelobjectZaakObject:
      allOf:
        - $ref: "#/components/schemas/ZaakObjectBase"
      properties:
        objectType:
          enum:
            - woz_deelobject
        objectIdentificatie:
          $ref: "#/components/schemas/WozDeelobjectObject"
    WozObjectZaakObject:
      allOf:
        - $ref: "#/components/schemas/ZaakObjectBase"
      properties:
        objectType:
          enum:
            - woz_object
        objectIdentificatie:
          $ref: "#/components/schemas/WozObjectObject"
    WozWaardeZaakObject:
      allOf:
        - $ref: "#/components/schemas/ZaakObjectBase"
      properties:
        objectType:
          enum:
            - woz_waarde
        objectIdentificatie:
          $ref: "#/components/schemas/WozWaardeObject"
    ZakelijkRechtZaakObject:
      allOf:
        - $ref: "#/components/schemas/ZaakObjectBase"
      properties:
        objectType:
          enum:
            - zakelijk_recht
        objectIdentificatie:
          $ref: "#/components/schemas/ZakelijkRechtObject"
    OverigeZaakObject:
      allOf:
        - $ref: "#/components/schemas/ZaakObjectBase"
      properties:
        objectType:
          enum:
            - overige
        objectIdentificatie:
          $ref: "#/components/schemas/OverigeObject"
    ZaakObjectVariantBase:
      oneOf:
        - $ref: '#/components/schemas/AdresZaakObject'
        - $ref: '#/components/schemas/BesluitZaakObject'
        - $ref: '#/components/schemas/BuurtZaakObject'
        - $ref: '#/components/schemas/EnkelvoudigDocumentZaakObject'
        - $ref: '#/components/schemas/GemeenteZaakObject'
        - $ref: '#/components/schemas/GemeentelijkeOpenbareRuimteZaakObject'
        - $ref: '#/components/schemas/HuishoudenZaakObject'
        - $ref: '#/components/schemas/InrichtingselementZaakObject'
        - $ref: '#/components/schemas/KadastraleOnroerendeZaakZaakObject'
        - $ref: '#/components/schemas/KunstwerkdeelZaakObject'
        - $ref: '#/components/schemas/MaatschappelijkeActiviteitZaakObject'
        - $ref: '#/components/schemas/MedewerkerZaakObject'
        - $ref: '#/components/schemas/NatuurlijkPersoonZaakObject'
        - $ref: '#/components/schemas/NietNatuurlijkPersoonZaakObject'
        - $ref: '#/components/schemas/OpenbareRuimteZaakObject'
        - $ref: '#/components/schemas/OrganisatorischeEenheidZaakObject'
        - $ref: '#/components/schemas/PandZaakObject'
        - $ref: '#/components/schemas/SpoorbaandeelZaakObject'
        - $ref: '#/components/schemas/StatusZaakObject'
        - $ref: '#/components/schemas/TerreindeelZaakObject'
        - $ref: '#/components/schemas/TerreinGebouwdObjectZaakObject'
        - $ref: '#/components/schemas/VestigingZaakObject'
        - $ref: '#/components/schemas/WaterdeelZaakObject'
        - $ref: '#/components/schemas/WegdeelZaakObject'
        - $ref: '#/components/schemas/WijkZaakObject'
        - $ref: '#/components/schemas/WoonplaatsZaakObject'
        - $ref: '#/components/schemas/WozDeelobjectZaakObject'
        - $ref: '#/components/schemas/WozObjectZaakObject'
        - $ref: '#/components/schemas/WozWaardeZaakObject'
        - $ref: '#/components/schemas/ZakelijkRechtZaakObject'
        - $ref: '#/components/schemas/OverigeZaakObject'
  securitySchemes:
    JWT-Claims:
      type: http
      bearerFormat: JWT
      scheme: bearer
  headers:
    ApiVersionHeader:
      schema:
        type: string
      description: |
        Geeft een specifieke API-versie aan in de context van een specifieke aanroep. Voorbeeld: 1.2.1.
    LocationHeader:
      schema:
        type: string
        format: uri
      description: URL waar de resource leeft.
    EtagHeader:
      schema:
        type: string
      description: |
        De ETag berekend op de response body JSON. Indien twee resources exact dezelfde ETag hebben, dan zijn deze resources identiek aan elkaar. Je kan de ETag gebruiken om caching te implementeren.
    ContentCrsHeader:
      schema:
        type: string
        enum:
          - EPSG:4326
      description: |
        Het 'Coordinate Reference System' (CRS) van de geometrie in de vraag (request body). Volgens de GeoJSON spec is WGS84 de default (EPSG:4326 is hetzelfde als WGS84).
tags:
  - name: klantcontacten
    description: ""
  - name: resultaten
    description: ""
  - name: rollen
    description: ""
  - name: statussen
    description: ""
  - name: zaakcontactmomenten
    description: ""
  - name: zaakinformatieobjecten
    description: ""
  - name: zaakobjecten
    description: ""
  - name: zaakverzoeken
    description: ""
  - name: zaken
    description: |
      Een zaak mag (in principe) niet meer gewijzigd worden als de `archiefstatus` een andere status heeft dan "nog_te_archiveren". Voor praktische redenen is er geen harde validatie regel aan de provider kant.
